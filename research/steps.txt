app snow removal, landscaping,etc. 


Project boot & infra
1.1 Create repo, branch strategy, README, LICENSE.
1.2 Provision Postgres DB (dev/local + staging).
1.3 Create .env.example with keys: DB, STRIPE_KEYS, S3, MAPBOX, NGROK_TOKEN.
1.4 Setup CI: linters + basic tests.

Data model & migrations
2.1 Implement tables: companies, contractors, properties, contracts, jobs, checkins, crews, users, invoices.
2.2 Add multi-service fields: service_type, pricing_model, service_meta (JSONB).
2.3 Add audit columns: created_by, created_at, updated_at, deleted_at.
2.4 Create seed script to load demo_data CSVs.

Core backend endpoints (MVP)
3.1 Auth: POST /auth/login, POST /auth/crew-login (simple token).
3.2 Import: POST /import/properties (CSV).
3.3 Contracts: GET /contracts, POST /contracts.
3.4 Jobs: POST /jobs (create), GET /jobs (list), PATCH /jobs/:id/status.
3.5 Check-ins: POST /jobs/:id/checkin (photo, lat, lon, ts, service_meta).
3.6 Tickets: GET /jobs/:id/ticket (HTML/PDF render).
3.7 Billing: POST /billing/subscribe, POST /billing/jobs/:job_id/invoice (Stripe test).
3.8 Search: GET /contractors?service_type=&lat=&radius=.

Frontend dispatcher (web) — 4 core screens
4.1 Dashboard: upcoming jobs, urgent alerts, quick-create job.
4.2 Property & Contracts: list, edit, create.
4.3 Calendar view: week/day; drag job to reschedule.
4.4 Job detail: job metadata, check-in timeline, ticket download, mark invoiced.

Mobile PWA (crew) — minimal flow
5.1 Simple login by crew code or phone+PIN.
5.2 Active jobs list (today).
5.3 Check-in screen: auto-capture GPS, allow photo(s) (min 1), optional service fields (notes, actual_volume, hours).
5.4 Offline mode: queue check-ins, sync when online.
5.5 Quick SOS / report-issue button (for emergencies).

Ticket PDF & template engine
6.1 Create Jinja2 or HTML templates per service_type.
6.2 Render photo thumbnails + GPS + map link + timestamps + crew name + contract details.
6.3 Produce downloadable PDF via WeasyPrint / wkhtmltopdf or HTML→PDF service.

CSV onboarding & admin tools
7.1 Provide CSV templates for properties, contracts, crews (exact columns separately).
7.2 Build admin upload UI with validation errors.
7.3 Add simple CSV preview before import.

Test, secure, deploy
8.1 Tests: unit tests for API flows; integration test for check-in → ticket PDF.
8.2 Security: image upload size limits, auth tokens, rate limits, HTTPS on staging.
8.3 Deploy: staging on Railway / Render / DigitalOcean; setup S3; use ngrok for contractor demo.
8.4 Monitoring: Sentry logs, DB metrics, Stripe webhooks for payment success.