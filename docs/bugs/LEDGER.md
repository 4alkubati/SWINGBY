# SwingBy bug ledger

> Generated by `python tools/bugctl.py render` @ 4d0deb9 on 2026-08-20.
> **Do not hand-edit** — the source of truth is `docs/bugs/ledger.jsonl`.

**9 live** · 81 fixed · 0 stale · 0 wontfix

## BLOCKER (1)

### SB-0023 — Live Google Maps API key is in PUBLIC git history and was never rotated — the same key is in mobile/.env today

- **status** `confirmed` · **area** `mobile` · found 2026-08-19 by audit-wf-72374f5f · checked 2026-08-19
- **files** `mobile/app.json:28 (in blobs cb5420afc4 / 9a8dbc70a7 / e3b3b156aa; commits 5c51dd0..000524b)`, `mobile/.env (untracked, holds the identical key)`, `mobile/app.config.js:11`, `mobile/.env.example`
- **evidence** [overnight audit resume wf_72374f5f — dimension: secrets and credential hygiene] code_state=committed confidence=confirmed NEEDS-HUMAN

gh repo view 4alkubati/SWINGBY --json visibility → {"isPrivate":false,"visibility":"PUBLIC"}

Commit 5c51dd0 (2026-05-29), message: "Hardening waves 1-12: ... real Maps key, Sentry DSNs"
Blob cb5420afc4, path mobile/app.json, line 28 (value masked by me):
    "googleMapsApiKey": "AIza<REDACTED_39CHAR_GOOGLE_KEY>",

The key survives in 3 distinct blobs (9a8dbc70a7, cb5420afc4, e3b3b156aa) across 5 commits, removed only at 000524b (2026-07-01, "Maps key placeholder").

All five commits are published:
  $ for c in 5c51dd0 f0d7635 57e960b 000524b 4820da5; do git merge-base --is-ancestor $c origin/main; done
  5c51dd0  reachable-from-origin/main: YES
  f0d7635  reachable-from-origin/main: YES
  57e960b  reachable-from-origin/main: YES
  000524b  reachable-from-origin/main: YES
  4820da5  reachable-from-origin/main: YES
  $ git log origin/main --oneline --find-object=cb5420afc4 -- mobile/app.json
  f0d7635 chore: sync local work to GitHub ...
  5c51dd0 Hardening waves 1-12: ... real Maps key, Sentry DSNs

NOT ROTATED — SHA-256 comparison against the key live in mobile/.env today:
  leaked key sha256[:12] = 6305d8bba3b6
  mobile/.env: 1 AIza key(s)
     sha256[:12]=6305d8bba3b6  SAME_AS_LEAKED=True

Key shape: len=39, 25 distinct chars after the AIza prefix, zero placeholder markers (no 'your'/'dummy'/'xxx'/'example'). mobile/.env.example calls this variable "THE ONE PLACE TO PASTE THE REAL KEY".

CONSEQUENCE: Anyone who clones or browses github.com/4alkubati/SWINGBY can recover a working Google Maps/Geocoding API key from history in seconds — automated GitHub secret scanners already do this continuously. The key is used both by the mobile app (mobile/.env → app.config.js:11) and server-side by backend/app/services/geocoding.py. Consequences run from billed quota theft (Geocoding/Places are metered per request against the project's card) to quota exhaustion that renders every map blank and silently kills server-side geocoding of new job posts. Removing it from HEAD at 000524b did nothing — a git blob stays fetchable forever once pushed.
- **verify** `Three checks, all must pass. Replaces the stored step, whose part (a) has a false-pass mode (if mobile/.env is missing or has no key, `re.search(...).group(0)` raises AttributeError and the script "does not print 6305d8bba3b6" — which reads as a pass) and whose part (b) needlessly punts the decisive check to a human console when it is fully automatable.

(a) The OLD key is DEAD at Google — this is the only check that proves remediation, and it needs no console. Pull the leaked value straight out of the public blob and probe it read-only:
  cd /home/l3thal/brain/10-swingby/swingby && git cat-file -p cb5420afc4 | python3 -c "
import sys,re,json,urllib.request,urllib.parse
k=re.search(r'AIza[0-9A-Za-z_-]{35}',sys.stdin.read()).group(0)
r=json.load(urllib.request.urlopen('https://maps.googleapis.com/maps/api/geocode/json?address=Calgary&key='+urllib.parse.quote(k),timeout=20))
print(r.get('status'))"
  → MUST print REQUEST_DENIED (today it prints OK). Any output of OK, or ZERO_RESULTS, or OVER_QUERY_LIMIT means the key still authenticates and the finding is NOT fixed.

(b) The local dev key is a different key, with an explicit failure if the file is missing:
  cd /home/l3thal/brain/10-swingby/swingby && python3 -c "
import re,hashlib,sys
s=open('mobile/.env').read()
m=re.search(r'AIza[0-9A-Za-z_-]{35}',s)
assert m, 'FAIL: no GOOGLE_MAPS_API_KEY in mobile/.env — inconclusive, not a pass'
print(hashlib.sha256(m.group(0).encode()).hexdigest()[:12])"
  → must print something OTHER than 6305d8bba3b6, and must not raise.

(c) The replacement is rotated everywhere the old one lived, not just locally — `eas env:list` must show GOOGLE_MAPS_API_KEY updated (as a secret) in both `preview` and `production`, and the Render backend env must be updated. Record the rotation date and the new key's restrictions as a row in docs/SECURITY.md; that row's existence is the artifact to check.

Optional (d), cleanup only, never a substitute for (a):
  git log --all --format=%H --find-object=cb5420afc4 -- mobile/app.json   # must output nothing
  gh api repos/4alkubati/SWINGBY/contents/mobile/app.json?ref=5c51dd0 --jq .sha   # must 404`
- **fix** Rotate first, purge second — in that order, because rotation is the only step that actually revokes access. (1) In Google Cloud console create a NEW key for project swingby-503604, apply Android package + SHA-1 / iOS bundle-id / HTTP-referrer restrictions and an API allowlist plus a quota cap, then DELETE the old key. (2) Update mobile/.env, the EAS `preview` and `production` environments (`eas env:update --name GOOGLE_MAPS_API_KEY`), and the backend Render env. (3) Optionally rewrite history (git-filter-repo --replace-text) and force-push, but treat that as cleanup, not remediation. (4) Enable GitHub push protection / secret scanning on the repo. (5) Add a row to docs/SECURITY.md recording the leak, the rotation date, and the new key's restrictions — there is currently no third-party credential inventory there at all.
- _note_ 2026-08-19: ADVERSARIAL VERIFY 2026-08-19 (wf_40a8b5fc): SURVIVED refutation. corrected_severity=critical. Stored verify step judged inadequate and replaced. I tried to break this one and could not — every load-bearing claim independently reproduced, and I proved one thing the original auditor left as "needs human".

1. Repo really is public: `gh repo view 4alkubati/SWINGBY --json visibility,isPrivate,pushedAt` → `{"isPrivate":false,"pushedAt":"2026-08-16T02:22:12Z","visibility":"PUBLIC"}`.

2. The leaked blob is served by GitHub right now, not just present in a local object store: `gh api repos/4alkubati/SWINGBY/contents/mobile/app.json?ref=5c51dd008e1061990c28f13162d26ac06033d074 --jq '.sha'` → `cb5420afc4f9cd42cdbcd882113829121a98db5e`. So it is fetchable by anyone over HTTPS, no clone needed.

3. Published on the real remote, not a stale local ref: `git ls-remote origin main` → `6cd2c5a56e25c547fed430740565c3a574a1dfe2`, and `git merge-base --is-ancestor 5c51dd0 6cd2c5a...` → true. The other blobs check out too: 9a8dbc70a7 in 57e960b + f0d7635, e3b3b156aa in 000524b + 57e960b, cb5420afc4 in 5c51dd0 + f0d7635 — the "3 blobs / 5 commits" count is accurate.

4. Same key, not rotated. Hashing (never printing) the `AIza[0-9A-Za-z_-]{35}` match: all three historical blobs give sha256[:12] = `6305d8bba3b6`, len 39, no placeholder markers; `mobile/.env` today gives exactly one key, sha256[:12] = `6305d8bba3b6`. Identical.

5. THE DECISIVE TEST THE AUDITOR DID NOT RUN. The obvious refutation was "a Maps key ships in the app bundle by design (docs/SECURITY.md:32), so if it is package+SHA-1 restricted the git leak adds nothing." I killed that argument with one read-only Geocoding GET from this box (no Android package, no referrer, arbitrary server IP), using the key from mobile/.env:
    https://maps.googleapis.com/maps/api/geocode/json?address=Calgary&key=<KEY>
    → status = OK, results = 1
The key is LIVE, unrevoked, and accepts requests from an arbitrary server with no restriction that would block a stranger. That is a billable Geocoding call anyone can make with a key pulled out of public history. This converts the "restriction unverified" mitigation into a confirmed absence of an effective restriction. (One request, ~$0.005, no mutation.)

Refutation angles I checked and that failed:
- Already logged elsewhere? No. docs/SECURITY.md:32 (D7.11) and Roadmap/HUMAN-TODO.md D12 both cover *restriction/quota* of Maps and Places keys — neither says the key is in public git history or that it needs rotation. This is a dist
- _note_ 2026-08-19: Adversarially verified 2026-08-19 (wf_40a8b5fc): survived refutation, mechanism reproduced against HEAD.

## BROKEN (3)

### SB-0076 — STALE/WRONG — HUMAN-TODO D13 is still flagged 'BREAKING EVERY BUSINESS' but the 500 was fixed in committed code on 2026-08-14, which changes the DEC-7 decision underneath it

- **status** `open` · **area** `backend` · found 2026-08-19 by audit-wf-40a8b5fc · checked never re-verified
- **files** `Roadmap/HUMAN-TODO.md:71`, `backend/app/api/subscriptions.py:85`
- **evidence** [overnight audit final wf_40a8b5fc — dimension: Backlog reconciliation — Roadmap/HUMAN-TODO.md, Roadmap] code_state=committed confidence=confirmed NEEDS-HUMAN

Roadmap/HUMAN-TODO.md:71 (committed): '| D13 | **TODO — BREAKING EVERY BUSINESS** | ... Consequence, proven live 2026-08-06: `POST /businesses/me/subscribe` returns **500** to an authenticated business owner.'

backend/app/api/subscriptions.py:63-96 (committed, `git log --oneline -- backend/app/api/subscriptions.py` → 693e661 'fix(security): D7.4–D7.13, the 5-try login, the last 3 walkthrough bugs, and the subscribe 500 (#158)', MERGED):

    # D13 — a misconfigured price id degrades to the beta posture instead of 500ing.
    ...
    if price_id and not price_id.startswith("price_"):
        env_name = "STRIPE_PRICE_SOLO" if tier == "solo" else "STRIPE_PRICE_TEAM"
        logger.error(...)
        price_id = None

    if not price_id:
        supabase.table("businesses").update({... "subscription_status": "trialing"}) ...
        return {"checkout_url": None, "status": "trialing", ...}

So `prod_Un0sRFGpiSHrL9` now falls through to the documented track-only beta posture and logs loudly, instead of 500ing.

CONSEQUENCE: Two distinct costs. (1) The team's single loudest open item is not open — a morning spent on it is wasted. (2) DEC-7 immediately below reads 'option (a) is one dashboard edit and fixes the 500 immediately'; that urgency is gone, so a pricing decision may get rushed on a premise that no longer holds. The Render env var is still wrong and still worth unsetting, but it is now a logging-hygiene item, not a user-facing outage.
- **verify** `Against a running backend with STRIPE_PRICE_TEAM set to a non-`price_` value, `POST /businesses/me/subscribe` as an authenticated business owner returns 200 with `status: "trialing"`, not 500 — and Roadmap/HUMAN-TODO.md D13 no longer says 'BREAKING EVERY BUSINESS'.`
- **fix** Re-mark D13 as CODE-FIXED / config-still-wrong, and rewrite DEC-7 to drop the 'fixes the 500 immediately' framing. Keep the ask to unset STRIPE_PRICE_TEAM in Render so the logger.error stops firing.
- _note_ 2026-08-19: DOC HALF FIXED in 516d1dc: HUMAN-TODO D13 no longer claims 'BREAKING EVERY BUSINESS' — verified against api/subscriptions.py:85, which degrades a bad price id to track-only instead of 500ing. DEC-7 re-marked as not-urgent. Stays OPEN for the DEC-7 pricing decision, which is a human call.

### SB-0077 — STILL OPEN — D19: www.swingbyy.com has no DNS record at all, confirmed live

- **status** `open` · **area** `docs` · found 2026-08-19 by audit-wf-40a8b5fc · checked never re-verified
- **files** `Roadmap/HUMAN-TODO.md:24`
- **evidence** [overnight audit final wf_40a8b5fc — dimension: Backlog reconciliation — Roadmap/HUMAN-TODO.md, Roadmap] code_state=n/a confidence=confirmed NEEDS-HUMAN

Roadmap/HUMAN-TODO.md:24: '| D19 | **TODO** | Cloudflare → DNS → `CNAME www → swingbyy.com`, proxied. `www.swingbyy.com` returns nothing (apex returns 200) — re-verified 2026-08-05.'

Re-verified 2026-08-19 from this box:

    $ getent hosts www.swingbyy.com   ->  (no output, exit 2)  "NO A RECORD"
    $ getent hosts swingbyy.com       ->  2606:4700:3033::6815:5351 / 2606:4700:3030::ac43:d9d8
    $ curl -s -o /dev/null -w '%{http_code}' --max-time 10 https://www.swingbyy.com/  ->  000 (exit 6, could not resolve host)

CONSEQUENCE: Unchanged in 14 days. Meta App Review needs a reachable privacy-policy URL, and any user typing the www. form — which is what people type — gets a DNS failure, not a redirect. This gates the Instagram/Facebook automation chain that D21/D23 are also waiting on.
- **verify** ``getent hosts www.swingbyy.com` returns an address AND `curl -s -o /dev/null -w '%{http_code}' https://www.swingbyy.com/` returns 200 or 301.`
- **fix** One Cloudflare DNS record: CNAME www → swingbyy.com, proxied. No agent has Cloudflare dashboard access.

### SB-0089 — About page lists two team members who may not exist — 'Jordan Li, Head of Engineering' and 'Maya Patel, Head of Operations'

- **status** `open` · **area** `web` · found 2026-08-20 by fix-session-2026-08-20 · checked never re-verified
- **files** `web/pre-launch/src/locales/en.json:about.member2Name`, `web/pre-launch/src/pages/About.jsx:16`
- **repro** Set VITE_PRELAUNCH_GATE=false and open /about — the team section lists three people.
- **evidence** Found 2026-08-20 while auditing pre-launch copy for the translation pass. About.jsx renders a three-person TEAM array: Amr Basem (Founder & CEO — real, the repo owner), 'Jordan Li, Head of Engineering' and 'Maya Patel, Head of Operations', each with a bio. Same shape and same page-set as the invented testimonials removed in a1e9446 and the invented use-case customers (Sarah/James/Priya). I CANNOT verify whether these two are real from inside the repo — there is no other reference to either name anywhere in the codebase, git history, or marketing/ — so unlike the testimonials I have not touched them. Flagging rather than guessing: deleting real colleagues is as wrong as publishing invented ones.
- **verify** `HUMAN: confirm whether Jordan Li and Maya Patel are real people who consent to being listed. If not, delete about.member2* and about.member3* and reduce TEAM in About.jsx to the real team.`
- **fix** If invented, remove both. A one-person team page that says one person is not a weakness; a team page with two people who do not exist is the same credibility problem as the testimonials.
- _note_ 2026-08-20: Left OPEN deliberately? No — see note.
- _note_ 2026-08-20: Closed by mistake on 2026-08-20 and immediately reopened — a stray close in a batch command. Nothing about it is fixed: About still lists 'Jordan Li, Head of Engineering' and 'Maya Patel, Head of Operations', now in French and Arabic as well. It needs a human to say whether they are real people.

## SLOPPY (4)

### SB-0028 — Cloudflare account ID and the operator's account-name email remain in published git history — the .gitignore comment claims this was closed, but untracking does not purge

- **status** `open` · **area** `infra` · found 2026-08-19 by audit-wf-72374f5f · checked never re-verified
- **files** `.gitignore ("Cloudflare Wrangler local state" block)`, `backend/.wrangler/cache/wrangler-account.json @4820da5 (blob 1489ba283a22f1862876d537ca4e0198764521e0)`, `workers/waitlist/.wrangler/cache/wrangler-account.json @4820da5 (blob 819ae4d316bdd8502d3ea661fdccb2816639fb8b)`
- **evidence** [overnight audit resume wf_72374f5f — dimension: secrets and credential hygiene] code_state=committed confidence=confirmed NEEDS-HUMAN

Four wrangler cache files were committed in 4820da5 (2026-05-22) and only untracked in e7a5ee2:
  A  backend/.wrangler/cache/pages.json
  A  backend/.wrangler/cache/wrangler-account.json
  A  workers/waitlist/.wrangler/cache/pages.json
  A  workers/waitlist/.wrangler/cache/wrangler-account.json

Structural inspection of the committed blobs (fields only, no values printed):
  backend/.wrangler/cache/wrangler-account.json
    /account/id    type=str len=32 kind=HEX32
    /account/name  type=str len=30 kind=EMAIL
  workers/waitlist/.wrangler/cache/wrangler-account.json
    /account/id    type=str len=32 kind=HEX32
    /account/name  type=str len=29 kind=EMAIL
  both pages.json: /account_id  len=32 kind=HEX32
No API token is present in any of the four — that is the good news.

commit 4820da5 is an ancestor of origin/main (verified), and the repo is PUBLIC.

The .gitignore text asserts the problem is handled:
  # Cache files carrying the Cloudflare account ID and the account-name email.
  # Four of these were tracked before this rule existed; they are untracked in the
  # same commit that adds it. This repo is PUBLIC — none of it should be readable.
That is true of HEAD and false of history.

CONSEQUENCE: Low direct impact — no token leaked, and the same Cloudflare Account ID (4877404e65143359d52e1056bfd8099c) and Zone ID are already published in the committed CLAUDE.md, so this adds only the operator's email address as a fresh disclosure (useful for targeted phishing against the Cloudflare account). The real cost is the misleading comment: a future reader takes "untracked in the same commit" as remediation and does not check history, which is exactly the reasoning error that left the Maps key in finding #1 unaddressed.
- **verify** `cd /home/l3thal/brain/10-swingby/swingby && git log --all --format=%H --find-object=1489ba283a22f1862876d537ca4e0198764521e0 | head -1
→ must output nothing after a history purge. If the decision is "accept, do not rewrite", then instead: grep -q 'remain in published history' .gitignore  → must succeed.`
- **fix** Either purge in the same git-filter-repo pass as the Maps key (finding #1) — one rewrite, one force-push, both problems — or, if history is being left alone, amend the .gitignore comment to say plainly "these four blobs remain in published history; the ignore rule only prevents new ones" so nobody re-reads it as closed. Enable 2FA + a hardware key on the Cloudflare account given the email is public.

### SB-0040 — Analytics collects nothing in production — VITE_POSTHOG_KEY is not set in the build

- **status** `confirmed` · **area** `infra` · found 2026-08-19 by audit-wf-72374f5f · checked 2026-08-19
- **files** `docs/DEPLOY.md:218`, `docs/DEPLOY.md:234`, `docs/RUNNING_LOCALLY.md:68`, `.github/workflows/web-launch-ci.yml:46`, `web/launch/.env.example:11`, `web/pre-launch/src/lib/analytics.js:24`
- **evidence** [overnight audit resume wf_72374f5f — dimension: external integrations — Cloudflare, Supabase, Render, Stripe] code_state=committed confidence=confirmed NEEDS-HUMAN

Deployed bundle: `grep -c -i posthog main.js` -> 0, and `grep -oE "phc_[A-Za-z0-9]{10,}" main.js` -> nothing. The snippet is not merely inert, it was tree-shaken out, which only happens when Vite statically substitutes an empty `import.meta.env.VITE_POSTHOG_KEY` at build time.

The call site is wired: web/pre-launch/src/main.jsx:10 `import { initAnalytics } from './lib/analytics'` and main.jsx:14 `initAnalytics()`. The gate is analytics.js:22-25:
  const KEY = import.meta.env.VITE_POSTHOG_KEY
  export function initAnalytics() { if (!KEY) return

The deploy runbook never mentions the variable. docs/DEPLOY.md:218 (frontend env-var table for web/launch):
  | `VITE_PLAUSIBLE_DOMAIN` | `swingbyy.com` |
and DEPLOY.md:234 in the go-live checklist:
  - [ ] Plausible domain configured
No VITE_POSTHOG_KEY row exists in either. Same in docs/RUNNING_LOCALLY.md:68 (`VITE_PLAUSIBLE_DOMAIN=    # leave empty locally`), web/launch/.env.example:11 (`# Analytics — Plausible (optional)`), and .github/workflows/web-launch-ci.yml:46 which sets `VITE_PLAUSIBLE_DOMAIN: swingbyy.com` and no PostHog var, so CI has never built the analytics path even once.

CONSEQUENCE: Commits 6cd2c5a/b5ff619 migrated code, CSP and subprocessor docs to PostHog on 2026-08-15, but the operator-facing half never moved. Anyone configuring a deploy from DEPLOY.md sets a variable nothing reads and omits the one thing that turns analytics on — and the failure is silent by design (`if (!KEY) return`). Production has been collecting zero funnel data since the migration, which is the data the October launch decisions are supposed to rest on. Note this is separate from the CSP finding: the CSP already allows the PostHog hosts, so fixing the key alone will work.
- **verify** `curl -s https://swingbyy.com/$(curl -s https://swingbyy.com/ | grep -oE 'assets/index-[A-Za-z0-9_-]+\.js' | head -1) | grep -c posthog   — must be > 0. Then confirm an event actually lands in the PostHog project; a key in the bundle only proves the snippet loaded.`
- **fix** Add VITE_POSTHOG_KEY (and optionally VITE_POSTHOG_HOST) to web/pre-launch/.env.production, which is TRACKED and is what the deploy builds from. Then redeploy and confirm events arrive in PostHog.
- _note_ 2026-08-19: ADVERSARIAL VERIFY 2026-08-19 (wf_40a8b5fc): SURVIVED refutation. corrected_severity=low. Stored verify step judged inadequate and replaced. I tried hard to break this one and the CORE FACT survived — but almost every supporting claim around it is wrong, and "blocker" is inflated by three levels.

WHAT I CONFIRMED (independently, not by trusting the auditor):
1. The live bundle really has no PostHog. `curl -s https://swingbyy.com/` names five app chunks; I fetched the entry chunk `https://swingbyy.com/assets/index-CsH_uuQv.js` (HTTP 200, 483877 bytes) and `grep -c -i posthog` -> **0**, `grep -oE 'phc_[A-Za-z0-9]{10,}'` -> nothing, `grep -c -i plausible` -> 0. I also fetched all four vendor chunks (vendor-i18n / vendor-motion / vendor-react / vendor-supabase): posthog=0 in every one. So it is not hiding in a split chunk.
2. The deployed build IS post-migration, which I had to prove because "the build is just old" was my best refutation. `curl -sI https://swingbyy.com/` returns `content-security-policy: ... script-src 'self' 'unsafe-inline' https://us-assets.i.posthog.com https://eu-assets.i.posthog.com; ... connect-src ... https://us.i.posthog.com ...`. That header comes from web/pre-launch/public/_headers, and `git show b5ff619~1:web/pre-launch/public/_headers | grep -c posthog` -> 0, i.e. the PostHog hosts entered that file only in b5ff619 (2026-08-15). Live edge is serving the post-migration _headers with a bundle that has no PostHog in it. Confirmed.
3. Docs really are stale: docs/DEPLOY.md:218 `| \`VITE_PLAUSIBLE_DOMAIN\` | \`swingbyy.com\` |`, DEPLOY.md:234 `- [ ] Plausible domain configured`, docs/RUNNING_LOCALLY.md:68 `VITE_PLAUSIBLE_DOMAIN=    # leave empty locally`, web/launch/.env.example:11-12, .github/workflows/web-launch-ci.yml:46 `VITE_PLAUSIBLE_DOMAIN: swingbyy.com`. All present at HEAD 5c1b029, all COMMITTED (not WIP — `git status` shows none of these files modified).

WHAT IS WRONG WITH THE FINDING AS WRITTEN — four errors:

(a) THE HEADLINE PROOF GREPPED THE WRONG FILE. Evidence says "Deployed bundle: `grep -c -i posthog main.js` -> 0". There is no `main.js` in this app — its chunks are `assets/index-<hash>.js`. The only `main.js` referenced by swingbyy.com's HTML is `/cdn-cgi/challenge-platform/scripts/jsd/main.js`, Cloudflare's bot-challenge script. The auditor almost certainly grepped Cloudflare's script and read it as the app bundle. The conclusion happens to be right — I re-derived it against the real entry chunk — but the stated proof does not support it, and the "it was t
- _note_ 2026-08-19: Adversarially verified 2026-08-19 (wf_40a8b5fc): core mechanism reproduces, but severity/consequence were overstated — downgraded. Re-read the title against the verdict note before fixing.
- _note_ 2026-08-19: DOC HALF FIXED in 516d1dc: DEPLOY.md and RUNNING_LOCALLY.md no longer tell operators to set VITE_PLAUSIBLE_DOMAIN. Stays OPEN for the real finding — the deployed bundle contains no PostHog code, which needs a rebuild/redeploy and a human to confirm events arrive.
- _note_ 2026-08-20: DIAGNOSIS CORRECTED 2026-08-20. The original wording ('deployed bundle contains no PostHog code') reads as missing code. The code is present and correct: src/lib/analytics.js loads PostHog from the CDN via the official snippet and starts with , so a build with no key deliberately ships nothing. posthog-js is intentionally NOT an npm dependency — that is documented in the file. The single cause is that VITE_POSTHOG_KEY is absent from web/pre-launch/.env.production, and the deploy workflow deliberately injects no env (an unset GitHub secret expands to an empty string and would OVERRIDE .env.production). Re-verified after tonight's deploy: 0 posthog references in the live bundle. Needs the real project key, which only a human has. The docs half — DEPLOY.md and RUNNING_LOCALLY.md telling operators to set the dead VITE_PLAUSIBLE_DOMAIN — is already fixed.

### SB-0081 — STILL OPEN — LAUNCH-BLOCKERS M4 (browser checkout fallback) is unchanged and was never re-tested as the doc asked

- **status** `open` · **area** `mobile` · found 2026-08-19 by audit-wf-40a8b5fc · checked never re-verified
- **files** `Roadmap/LAUNCH-BLOCKERS.md:102`, `mobile/src/services/acceptAndPay.js:149`
- **evidence** [overnight audit final wf_40a8b5fc — dimension: Backlog reconciliation — Roadmap/HUMAN-TODO.md, Roadmap] code_state=committed confidence=confirmed NEEDS-HUMAN

Roadmap/LAUNCH-BLOCKERS.md:102-105 M4 · S2 · OPEN, and the recount at :254 says 'per the note below it largely disappears now that M2/M3 have landed, so it should be re-tested before any further work is spent on it.'

The code is unchanged: mobile/src/services/acceptAndPay.js:113 `checkoutUrl = res?.checkout_url || null;` and :149 `await Linking.openURL(url);`. The file's own header comment at :7 still describes 'calling Linking.openURL. That is the busier of the two payment paths'. No re-test has been recorded anywhere in the three roadmap docs since 2026-08-02.

CONSEQUENCE: Genuinely S2 only if the native sheet is unavailable, and M2/M3 both landed, so the fallback should now be rare. But 'should now be rare' has never been measured, and the doc explicitly asked for a measurement 17 days ago. The failure mode — a user leaves the app to a browser and does not come back — is where the walkthrough's unpaid bookings came from.
- **verify** `Roadmap/LAUNCH-BLOCKERS.md M4 carries a dated re-test result (device, sheet-vs-browser outcome), not a prediction — or acceptAndPay.js no longer contains a `Linking.openURL(url)` payment fallback.`
- **fix** Run the accept-and-pay flow on the preview build and record how often the native sheet path is taken vs. the Linking.openURL path. If the sheet is reliable, close M4; if not, M4 is still a real day-one issue.

### SB-0082 — STILL OPEN — DEC-6 / M11 credit redemption remains gated off, unchanged since 2026-08-04

- **status** `open` · **area** `backend` · found 2026-08-19 by audit-wf-40a8b5fc · checked never re-verified
- **files** `backend/app/services/credits.py:44`, `Roadmap/HUMAN-TODO.md:69`, `Roadmap/LAUNCH-BLOCKERS.md:123`
- **evidence** [overnight audit final wf_40a8b5fc — dimension: Backlog reconciliation — Roadmap/HUMAN-TODO.md, Roadmap] code_state=committed confidence=confirmed NEEDS-HUMAN

backend/app/services/credits.py:44 (committed): `CREDIT_REDEMPTION_AT_CHECKOUT_ENABLED = False`. Both consumers still branch on it and both are dead today: backend/app/api/payments_stripe.py:176 `if credits.CREDIT_REDEMPTION_AT_CHECKOUT_ENABLED:` and backend/app/services/payment_triggers.py:172, same expression. Roadmap/HUMAN-TODO.md DEC-6 and Roadmap/LAUNCH-BLOCKERS.md M11 both describe exactly this state, so the docs are accurate — this is confirmation, not drift.

CONSEQUENCE: A client owed a $25 goodwill credit can see the balance and cannot spend it; the in-app copy tells them to contact support, so every credit is a support ticket. Unchanged for 15 days. The doc also records a known hole (an abandoned checkout keeps the credit spent) that must be closed before the flag flips.
- **verify** ``grep -n 'CREDIT_REDEMPTION_AT_CHECKOUT_ENABLED' backend/app/services/credits.py` shows `= True` AND a test exists asserting that an abandoned checkout restores the credit balance.`
- **fix** Decision, not code: verify the capture path in Stripe test mode end to end, close the abandoned-checkout hole, then flip the constant. Nothing here can be done without the Stripe test-mode decision.

## POLISH (1)

### SB-0084 — HUMAN-ONLY — 18 backlog items confirmed as still requiring dashboard access or a business decision no agent can make

- **status** `open` · **area** `docs` · found 2026-08-19 by audit-wf-40a8b5fc · checked never re-verified
- **files** `Roadmap/HUMAN-TODO.md:22`, `docs/SECURITY.md:6`
- **evidence** [overnight audit final wf_40a8b5fc — dimension: Backlog reconciliation — Roadmap/HUMAN-TODO.md, Roadmap] code_state=n/a confidence=confirmed NEEDS-HUMAN

Confirmed as genuinely agent-blocked, current status unchanged as of 2026-08-19:

Apple (blocked behind incorporation, per the ordering warning at HUMAN-TODO.md:29-47): D15 create the App Store Connect record, D16 ascAppId + Apple ID email (mobile/eas.json still holds REPLACE_WITH_* — grep confirms), D17 App Store Connect API key .p8, D18 Paid Apps agreement.
Social: D20 display names ('AMR' on Instagram, 'Swing' on X), D21 convert Instagram to Business, D22 tokens in .env.social, D23 YouTube @swingbyy.
Google: D25 create+verify a Google Business Profile (~11-14 weeks), D26 Business Profile API access — HUMAN-TODO.md:99 records gcloud returning 'Service mybusiness.googleapis.com is not available to this consumer'. Code side is correctly defensive: backend/app/api/google_reviews.py:179 `os.getenv("GOOGLE_REVIEWS_ENABLED", "")` defaults OFF, so the 'do NOT set it' warning is safe.
Decisions: DEC-2 (incorporation entity + registered address), DEC-3 (Apple Pay merchant id — mobile/app.config.js:126 `process.env.STRIPE_MERCHANT_IDENTIFIER || ''` is correctly empty and must stay empty), DEC-7 (Stripe catalog is empty), DEC-8 (Android vs iOS for the laptop-free build), DEC-9 (which site swingbyy.com serves), DEC-10 (link in bio).
docs/SECURITY.md dashboard rows: HIBP leaked-password protection (disabled), email confirmation toggle, email-link expiry TTL, Google Maps key restriction (D7.11).

CONSEQUENCE: None of these can move without Kira. Listing them here so the morning triage can separate 'I have to do this' from 'an agent will do this' — which is the whole point of this reconciliation.
- **verify** `Each row in Roadmap/HUMAN-TODO.md carries a status dated 2026-08-19 or later; specifically D15's status changes only after an App Store Connect record exists and mobile/eas.json no longer contains REPLACE_WITH_ (`grep -c REPLACE_WITH_ mobile/eas.json` → 0).`
- **fix** No code action. The only ordering constraint worth re-reading is HUMAN-TODO.md:29-47: incorporate → D-U-N-S → enrol as organization → then D15-D18, now that D14 confirmed the enrolment is Individual.

---

## Closed (81)

- `SB-0001` **fixed** 2026-08-19 — Quote thread header says 'pending payment' while the accepted card below it says 'paid'
- `SB-0002` **fixed** 2026-08-19 — Business profile-completeness hint is truncated mid-sentence with no way to read it
- `SB-0003` **fixed** 2026-08-19 — Business setup trades do not populate 'Services & pricing' — a categorised business shows 'No services listed yet'
- `SB-0004` **fixed** 2026-08-19 — Public-profile preview shows a back arrow underneath the 'Done' preview banner — two conflicting exits
- `SB-0005` **fixed** 2026-08-19 — Owner's own review renders on the business public profile as a Swingbyy review
- `SB-0006` **fixed** 2026-08-19 — 'Propose a time' CTA shown for bookings whose date is already confirmed
- `SB-0007` **fixed** 2026-08-19 — Business dashboard renders hardcoded English literals in every locale
- `SB-0008` **fixed** 2026-08-19 — Language badge reads EN while Ukrainian is the checked selection
- `SB-0009` **fixed** 2026-08-19 — A booking reaches 'Job complete' with zero proof-of-work photos
- `SB-0010` **fixed** 2026-08-19 — Booking timeline logs 'Date confirmed' four hours before the events it precedes, with a posting time in the future
- `SB-0011` **fixed** 2026-08-19 — Rate limiter is keyed on the attacker-controlled X-Forwarded-For header — every per-IP limit is bypassable
- `SB-0012` **fixed** 2026-08-19 — slowapi default_limits never fires — no SlowAPIMiddleware registered, so undecorated routes have no rate limit at all
- `SB-0013` **fixed** 2026-08-19 — The M-05 fix silently rejects every declared image/heic upload — allowlist and sniff table disagree in committed code
- `SB-0014` **fixed** 2026-08-19 — GET /businesses/* returns the full row — Stripe Connect id, customer id, subscription id and license number — to any authenticated user in bulk
- `SB-0015` **fixed** 2026-08-19 — businesses.logo_url still accepts metadata/loopback URLs — the M-04 fix never reached this sink
- `SB-0016` **fixed** 2026-08-19 — employees.avatar_url has no URL validation at all — accepts javascript: and cloud-metadata URLs
- `SB-0017` **fixed** 2026-08-19 — Brute-force guard's per-IP arm reads the spoofable X-Forwarded-For — 30 spoofed failures lock out any chosen IP
- `SB-0018` **fixed** 2026-08-19 — M-02 (rate limit on POST /messages/) is the only pentest fix with no regression test
- `SB-0019` **fixed** 2026-08-19 — WIP login lockout is fail-open — a PostgREST outage silently disables brute-force protection with no alert
- `SB-0020` **fixed** 2026-08-19 — M-01 residual: redirect allowlist matches a scheme PREFIX, so a bare-scheme env value re-opens auth-code harvesting
- `SB-0021` **fixed** 2026-08-19 — Production CORS allowlist unconditionally includes localhost:5173 and :3000 with allow_credentials=True
- `SB-0022` **fixed** 2026-08-19 — 429 responses use an `error` key while every other API error uses `detail` — clients render an empty message
- `SB-0024` **fixed** 2026-08-19 — The "anon" Supabase auth client silently falls back to the SERVICE-ROLE key when SUPABASE_KEY is empty — which is the state of backend/.env today
- `SB-0025` **fixed** 2026-08-19 — render.yaml declares 7 env vars while the backend reads 39 — every payment, email, captcha and error-reporting secret is absent from the only committed deploy manifest
- `SB-0026` **fixed** 2026-08-19 — .gitignore does not cover .env.production / .env.staging / .env.preview — a backend prod env file would be committed silently
- `SB-0027` **fixed** 2026-08-19 — backend/ has no .env.example — the only backend env documentation is a 6-line block in RUNNING_LOCALLY.md that names a variable the code never reads and omits 30 it does
- `SB-0029` **fixed** 2026-08-19 — EXPO_PUBLIC_GOOGLE_MAPS_API_KEY is read by the app but set and documented nowhere — setting it (the obvious fix for the dead autocomplete) inlines the Maps key into the JS bundle
- `SB-0030` **fixed** 2026-08-19 — Web .env.example drift: two vars used but undocumented, two documented but never read
- `SB-0031` **fixed** 2026-08-19 — axios 1.16.1 ships in the mobile app, the live swingbyy.com site and web/admin — 10 published advisories including prototype pollution that alters request construction
- `SB-0032` **fixed** 2026-08-19 — react-router open redirect + XSS unpatched in all three web apps, while docs/SECURITY.md records the fix as complete
- `SB-0033` **fixed** 2026-08-19 — `npm audit` runs in CI for exactly one app and it is the one that is never deployed; mobile CI passes --no-audit explicitly
- `SB-0034` **fixed** 2026-08-19 — Backend has no dependency lock at all — 10 of 15 runtime deps are unpinned and the Dockerfile resolves them at image-build time
- `SB-0035` **fixed** 2026-08-19 — Every CI and deploy workflow pins Node 20, which reached end-of-life on 2026-04-30 — 110 days ago
- `SB-0036` **fixed** 2026-08-19 — Dev-tooling CVEs in the backend venv: sqlparse 0.5.5 carries 4 advisories, and CI hard-pins black to an advisory-affected version
- `SB-0037` **fixed** 2026-08-19 — stripe SDK is capped at <12 while 15.5.1 is current — four majors behind, and the ceiling means Dependabot cannot even propose the move
- `SB-0038` **fixed** 2026-08-19 — Mobile's 19 'high' npm advisories are all build-tooling, not shipped code — but Expo SDK 54 / RN 0.81.5 drift has no owner at all
- `SB-0039` **fixed** 2026-08-19 — Production waitlist form is blocked by its own CSP — connect-src omits api.swingbyy.com
- `SB-0041` **fixed** 2026-08-19 — Cloudflare waitlist Worker returns the raw Notion API error body to any anonymous caller — the exact leak its FastAPI twin documents having fixed
- `SB-0042` **fixed** 2026-08-20 — Stripe Connect account ids on `businesses` are readable by any authenticated Supabase JWT — the 2026-07-20 column REVOKE was never extended to the 2026-08-03 columns
- `SB-0043` **fixed** 2026-08-20 — `push_tokens` and `audit_log` are written by the API but have no CREATE TABLE and no RLS statement anywhere in the repository
- `SB-0044` **fixed** 2026-08-20 — Schema split-brain: the `stripe_events` idempotency table the webhook hard-depends on lives only in docs/, outside the migration pipeline
- `SB-0045` **fixed** 2026-08-19 — /healthz — Render's healthCheckPath — is a hardcoded 200 that cannot fail, so a deploy with broken Supabase credentials is promoted as healthy
- `SB-0046` **fixed** 2026-08-19 — hCaptcha is a one-way switch that breaks 100% of signups — the backend enforces it but no client ever sends a token
- `SB-0047` **fixed** 2026-08-19 — Backend integration SDKs are entirely unpinned with autoDeploy on and no lockfile — Supabase, FastAPI, httpx and notion-client float to latest on every build
- `SB-0048` **fixed** 2026-08-19 — web/launch's Supabase client is the unguarded version that blanked swingbyy.com on 2026-08-04 — the fix landed only in web/pre-launch
- `SB-0049` **fixed** 2026-08-19 — Production runs Python 3.11 while all local development and the 1208-test suite run 3.14
- `SB-0050` **fixed** 2026-08-19 — CLAUDE.md documents a Notion CRM integration (notion_crm.py, NOTION_CRM_DB_ID) that was deleted and no longer exists
- `SB-0051` **fixed** 2026-08-19 — No runbook exists for deploying the production waitlist Worker or setting its NOTION_TOKEN secret
- `SB-0052` **fixed** 2026-08-20 — `main` has no branch protection and no rulesets — every workflow, including the live-site gate, is purely advisory
- `SB-0053` **fixed** 2026-08-19 — web/pre-launch — the only app in production — has 6 HIGH npm vulnerabilities and no CI audit step; the audit gate exists only on the app that is never deployed
- `SB-0054` **fixed** 2026-08-19 — The pass-3 WIP will fail Backend CI's lint job, and `test: needs: lint` means all 1170 backend tests are then skipped, not run
- `SB-0055` **fixed** 2026-08-20 — Dependabot covers no web app at all, and Dependabot security alerts are disabled repo-wide — nothing would ever have surfaced the live site's 6 highs
- `SB-0056` **fixed** 2026-08-19 — The backend secret scan only runs when web/launch changes, so it did not run for the 19-file security commit it was meant to protect
- `SB-0057` **fixed** 2026-08-19 — web-launch CI has been red on `main` since 2026-08-05 — three merges landed on top of a standing failure
- `SB-0058` **fixed** 2026-08-19 — web-prelaunch Deploy reports FAILURE on every successful production deploy — the alarm on the only workflow that ships the live site is permanently red
- `SB-0059` **fixed** 2026-08-19 — web CI builds the production site with a different API host than the deployed bundle uses, so a green run does not validate the shipped configuration
- `SB-0060` **fixed** 2026-08-19 — No web app contains a single test file; every web CI test step is a silent no-op or a stub that is never invoked
- `SB-0061` **fixed** 2026-08-19 — web-launch-ci.yml triggers on push to every branch, doubling every PR's runs; web-ci.yml and web-launch-ci.yml also lack concurrency groups
- `SB-0062` **fixed** 2026-08-19 — No CODEOWNERS, no PR template, and the two web workflows declare no `permissions:` block
- `SB-0063` **fixed** 2026-08-19 — tools/e2e_smoke.py is called mandatory by DISPATCH_GATE Layer 6 but runs in no workflow
- `SB-0064` **fixed** 2026-08-19 — Reconciliation: 8 Dependabot PRs and 1 security PR sit open and unmerged, and docs/DEPLOY.md's CI inventory is stale
- `SB-0065` **fixed** 2026-08-19 — backend/tests/test_smoke_prod.py hits the live production API on every CI run — the "skipped by default" it documents was never implemented
- `SB-0066` **fixed** 2026-08-19 — docs/API.md documents 51 endpoints against a 144-endpoint API, and 7 of the documented routes do not exist
- `SB-0067` **fixed** 2026-08-19 — docs/SECURITY.md claims "All routes auth-protected — 2 intentionally open" while 11 route handlers take no auth dependency, including all four social-auth endpoints
- `SB-0068` **fixed** 2026-08-20 — docs/SECURITY.md says the post-expiry cron is "Live on Supabase" — two source files state flatly that no scheduler exists in this deployment
- `SB-0069` **fixed** 2026-08-19 — docs/swingby_database_schema.md — the schema authority CLAUDE.md points at — is missing 16 tables, including both moderation tables
- `SB-0070` **fixed** 2026-08-19 — web/launch builds production sourcemaps with full original source embedded and deploys the directory verbatim to Cloudflare Pages
- `SB-0071` **fixed** 2026-08-19 — login_guard.prune_attempts() has no caller anywhere — the migration's own COMMENT promises a 7-day retention sweep that nothing executes
- `SB-0072` **fixed** 2026-08-19 — mobile/src/i18n.js comments still describe Arabic and French as untranslated skeletons — all four locales are at 100% key coverage
- `SB-0073` **fixed** 2026-08-20 — STILL OPEN — privilege escalation to platform admin is in no backlog document; its only fix is unmerged PR #159 and the migration is absent from the tree
- `SB-0074` **fixed** 2026-08-19 — STILL OPEN — the live pg_cron expires posts to a status the refund sweep is coded to skip, so escrow on an expired post can never be refunded
- `SB-0075` **fixed** 2026-08-19 — STALE/WRONG — docs/SECURITY.md's committed refresh-token retention policy is not enforced: the prune runs only from an admin-only endpoint and there is no scheduler
- `SB-0078` **fixed** 2026-08-19 — STALE/WRONG — docs/SECURITY.md 'RLS on all 10 tables' has been outgrown: 13 further tables carry their own RLS migrations and the row can no longer be read as coverage
- `SB-0079` **fixed** 2026-08-19 — STILL OPEN — OPEN-2026-08-12 item 3: expo-splash-screen is still not installed and nothing holds the native splash across mount
- `SB-0080` **fixed** 2026-08-19 — STILL OPEN (mitigated) — DEC-5's recorded webhook gap is real: account.updated, payout.*, transfer.* are still ignored; reconcile-on-read is what saves it
- `SB-0083` **fixed** 2026-08-19 — STALE — OPEN-2026-08-12 item 4 (branch hygiene) has already drifted: 6 new undeleted branches, and the 'origin'-named branch it flagged is gone
- `SB-0085` **fixed** 2026-08-19 — web/launch CSP has no frame-ancestors — clickjacking defence rests on the deprecated X-Frame-Options alone
- `SB-0086` **fixed** 2026-08-19 — node_modules is committed as an ABSOLUTE symlink for two web apps — breaks every clone, worktree and CI runner but this box
- `SB-0087` **fixed** 2026-08-20 — One Expo push token is registered to 4 different users — notifications leak across accounts on a shared device
- `SB-0088` **fixed** 2026-08-20 — 29 pre-launch strings live as inline i18next defaults — invisible to the locale files, so fr/ar can never translate them
- `SB-0090` **fixed** 2026-08-20 — Arabic shipped in both web apps with no dir=rtl — the page reads backwards

