---
group: build
project: swingby
hub: "[[MOC-Build]]"
tags: [build]
---
# QA Session — 2026-07-18 (Kira on-device, Android)

> Tracker for Kira's design-handoff debugging session. Status flow:
> **reported → diagnosed → fix-queued (overnight) → fixed → ready-to-retest → pass/fail.**
> Design reference: `design/handoff-mocks-2026-07-17/swingby-mocks-standalone.html` + 31-screen exec spec
> (`~/brain/projects/swingby/claude/deliverables/design-exec-spec-2026-07-18.md`).
> Screenshot inbox: `~/brain/inbox/` (image1 = current Profile screen; image2/design ref not yet received).

## Bugs

| ID | Screen / area | Report (Kira's words, condensed) | Triage so far | Status |
|---|---|---|---|---|
| QA-01 | Client Search / browse | "I can't search/browse for a company" | `SearchScreen.js` exists & reachable (Home → Search → BusinessProfile) but it only calls `GET /businesses/nearby` — **no name-based business search exists in the backend at all**. Also geo-driven, so QA-03's Android location failure makes it look completely dead. | **fixed (2026-07-18 overnight)** — `GET /businesses/?q=` name search added (ilike, wildcards escaped, no location required); `SearchScreen.js` now calls it and renders results with coords absent (distance simply omitted). Orchestrator review caught a follow-on: the category filter used `.eq()` against capitalized labels while mobile sends lowercase ids → fix queued to backend-agent. → ready-to-retest |
| QA-02 | Profile (`profile/ProfileScreen.js`) | Doesn't match the design handoff (image1 = current state) | Spec entry exists in the 31-screen exec spec; implement its Profile gap list. image2 (design ref) never landed in inbox — spec + standalone mocks are the source. | fix-queued |
| QA-03 | Address input, Android | "on the Android I can't use the address" | Device-compat issue; likely Places/autocomplete or keyboard behavior on Android. Needs code audit of PostJob address field + anywhere address is entered. Cascades into QA-01 (geo search empty). | **fix applied, cause-plausible not device-confirmed (2026-07-19 overnight)** — full audit of every address entry point (profile address edit turned out not to exist; `location.js` is a pure GPS wrapper, innocent). Ruled out with evidence: zIndex/elevation (the library never positions the list absolutely — it renders inline), Places API key (unset on BOTH platforms, so it can't explain an Android-only failure), KeyboardAvoidingView inconsistency (left alone rather than guess-patched). Root cause found: `GooglePlacesAutocomplete` nests a scrollable results FlatList inside each screen's own ScrollView — the "VirtualizedList nested in a ScrollView, same orientation" anti-pattern that Android's stricter gesture arbitration breaks and iOS tolerates. Fixed with `disableScroll` (PostJobScreen.js:346, BusinessSetupScreen.js:171), handing scrolling to the parent. Babel 116/0. **Caveat:** a prior pass already fixed the controlled-`value` IME conflict on these same fields and that alone did NOT close Kira's report — so this may also be partial. Repro question parked in HUMAN-TODO. → ready-to-retest |
| QA-04 | Time picker, Android | "time clock on the Android failed — click OK and the box doesn't update" | Classic `@react-native-community/datetimepicker` Android bug: on Android the picker is a one-shot dialog and the value must be committed in `onChange` (event.type === 'set'), not on iOS-style dismiss. High-confidence fix. | **fixed (2026-07-18 overnight)** — audited both DateTimePicker call-sites: `PostJobScreen.js` `onTimeChange` now hides the Android dialog and commits only on `event.type === 'set'`; `ConfirmDateCard.js` was already compliant (bails on `dismissed`). iOS confirm flow unchanged. → ready-to-retest |
| QA-05 | Photos, business side | "client uploads pictures but the business can't see them" | `service_posts.image_urls` exists; check the business feed (Dashboard/post detail) actually renders them, and `booking_photos` render on JobManagement. Likely render-side omission. | **fixed (2026-07-18 overnight)** — confirmed render-side, backend was innocent (`service_posts` and `booking_photos` both `select("*")`, and JobManagement already mounts `BookingPhotos`). Real cause: `JobOpportunityCard.js` (Dashboard opportunity cards) and `SendQuoteSheet.js` (post detail before quoting) never referenced `post.image_urls` at all. Both now render thumbnail strips (i18n'd, 3 locales). Noted for later: `/interests/mine` column-selects without `image_urls` — harmless today, will bite if a quoted-post detail is added. → ready-to-retest |
| QA-06 | Messaging flow | "quote thread auto-deletes after the handshake and flips to a new chat — flow is wrong by miles" | Design intent (CLAUDE.md): **one continuous thread across the quote → booking arc**. Current behavior orphans the pre-booking history when the booking chat starts. Unify: same thread, booking-details card appended at handshake, history preserved. | **fixed (2026-07-18 overnight)** — root cause was NOT message loss: `interests.py:317` already re-parents the quote thread's message rows onto the booking on accept, so history was never deleted. What vanished was the `<job title> · $X quoted` context line, which only ever arrived via `GET /messages/interest/*`; the moment the thread flipped to booking_id routing it went blank, which read on-device as "a new chat". `messages.py` `_quote_context_for_booking()` now returns that same `interest` shape from `GET /messages/{booking_id}` (best-effort, read-only, None for direct-browse bookings). Orchestrator-verified: `ChatScreen.js:319` consumes `data?.interest` unchanged, so no mobile edit was needed. Pending pytest green (DQ-8) + morning e2e_smoke vs Render. → ready-to-retest |
| QA-07 | Handoff fidelity batch | Screens not built to handoff: `business/BusinessProfileScreen.js` (owner "My Business" view **and** public browse view — clients browse it too), `business/JobManagementScreen.js` | Implement per exec-spec entries. **Kira's product call (2026-07-18): JobManagement gets brought to the handoff mock** — this answers the spec's flagged #26 question. | fix-queued |
| QA-08 | Client My Jobs (`client/MyJobsScreen.js`) | "my jobs in client side looks super messy" | Bring to handoff per exec spec. | fix-queued |

## Product notes captured (not bugs)

- **Escrow/handshake direction (Kira, confirmed):** handshake originates from the **business** side; when the **client confirms, they pay** and funds are held in escrow until the job is done. Verify current flow matches; flag any divergence.
- **FEATURE-01 — Route optimization (parked, post-debug):** if a business has multiple jobs the same day with no fixed times, suggest the best visit order from job locations ("which house to hit first"), Uber-driver style. Needs product design + likely a maps/optimization backend. Captured in brain PLAN Parked.
- **TOOL-01 — Pre-doctor deep diagnostic:** containerized test-runner that boots the backend, exercises the app flows (pytest + babel + flow graph + e2e smoke), and produces a failure report after each session — runs *before* the doctor so the morning brief carries real diagnosis, not just health pings. First version queued tonight.

## Retest checklist (Kira, next session)

- [ ] Search a business by name from Home → results appear without location
- [ ] Profile screen matches handoff
- [ ] Android: address field usable end-to-end
- [ ] Android: time picker OK button commits the chosen time
- [ ] Business side: client's post photos visible; job photos visible on JobManagement
- [ ] Send quote → accept → chat history from the quote still visible in the booking thread
- [ ] BusinessProfile (owner + public) and JobManagement match handoff
- [ ] Client My Jobs matches handoff

<!-- graph-wire:start -->
---
**Up:** [[MOC-Build]] · **Home:** [[SWINGBY]]

**Related:** [[2026-07-18]] · [[2026-07-19]] · [[CLAUDE]]
<!-- graph-wire:end -->
