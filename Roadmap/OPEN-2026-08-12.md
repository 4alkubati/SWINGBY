# Open engineering items — 2026-08-12

Found while reconciling branches against `main` and preparing the preview build.
Kira-only actions are **not** here — those are H27–H30 in `HUMAN-TODO.md`.

---

## 1. A money guard that was written, tested, and never merged

**Branch `fix/w3-unassigned-complete-guard` — 6 commits, never opened as a PR.**

Most of it reached `main` by other routes. Verified individually rather than
assumed, because commit subjects are not evidence:

| Fix | In `main`? | Evidence |
|---|---|---|
| `payment_status` never left `pending` after capture | **yes** | `payments_stripe.py:750` — "Bug 4 (walkthrough)", same numbering |
| Garbage ids 500 instead of 404 | **yes** | `main.py:104` — the `22P02` handler |
| Duplicate `completed` booking_event | assumed yes | same walkthrough batch |
| **A booking with nobody assigned completes and releases escrow** | **NO** | `git grep -c 'not booking.get("employee_id")'` → **1** on the branch, **0** on `main` |

The missing one is the money-relevant one. Its own comment on the branch places
it in the same family as the "business paid itself" bug:

> *A booking nobody was ever assigned to completed and released escrow exactly
> like an assigned one.*

**Do not cherry-pick it blind.** The branch's merge-base is `ed66815` and the
completion path has changed since — `main` now has an employee identity check at
`bookings.py:1130` (`emp.data["id"] != booking.get("employee_id")`), which is a
*different* check: it verifies *who* is assigned, not *that anyone is*. A null
`employee_id` fails neither. Confirm the hole still reproduces against current
`main` before porting anything.

Per `CLAUDE.md`, anything touching this path needs `tools/e2e_smoke.py` green
(DISPATCH_GATE Layer 6), and the caller list for escrow functions must be
confirmed with `grep`, not `graphify affected` — that under-reports
module-qualified calls and has already cost a half-shipped money fix.

## 2. `prep/ios-testflight` was held back on purpose, and the reason expired

One commit, never PR'd. Its own message explains why it sat:

> *ON A BRANCH, NOT MAIN: another agent is mid debugging-walkthrough with a
> locked worktree off the same commit. Merge this when their run is done.*

That run finished long ago. The branch adds the `testflight` build profile and
iOS submit config; it touches no app source. Nothing depends on it for the
**preview** build — `HUMAN-TODO.md` records that the current loop runs on the
installable preview build, not TestFlight — but it should stop being a loose end.

## 3. `expo-splash-screen` is not installed

The white-screen fix (`App.js`, 2026-08-12) stops the app rendering `null`, so a
stalled font load now shows a dark branded spinner instead of a white void. It
does **not** close the underlying gap: nothing holds the native splash across the
mount, so there is still a brief unstyled frame on cold start.

Deliberately deferred — adding it is a new native module and a prebuild change,
which is not something to do hours before a build. Worth doing on a normal day,
paired with `SplashScreen.preventAutoHideAsync()` at module scope and
`hideAsync()` once fonts settle.

**Still unknown: why the fonts stalled at all.** The font packages are intact on
disk (TTFs present, exports resolve) and the timeout now masks the symptom. The
root cause needs the Metro console from a dev-client run — `npx expo start
--dev-client`, open the build against it, read the error. Left for the debugging
session Kira planned for when they are home.

## 4. Branch hygiene — 33 branches merged and never deleted

Cross-referenced every remote branch against its PR state:

- **33** have a MERGED PR → dead weight, safe to delete.
- **7** have an OPEN PR (4 of them dependabot: #72, #75, #78, #106).
- **1** closed unmerged: `feat/d5-payouts-stripe-connect` (PR #97). **Superseded,
  not lost** — `HUMAN-TODO.md` H21 records that the D5 code reached `main` via
  PR #98 instead, and notes the confusion that caused when the migration row
  still said "only needed when #97 merges". Safe to delete.
- **2** with no PR ever → items 1 and 2 above.

Note for anyone repeating this: `git log origin/main..branch` and
`git diff origin/main..branch` both lie here, because the repo squash-merges and
`main` has moved far ahead — every stale branch looks like it holds hundreds of
unmerged files. PR state is the only reliable signal.
