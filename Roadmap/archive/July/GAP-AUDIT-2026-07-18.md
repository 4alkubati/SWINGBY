# Gap Audit — 2026-07-18 (full app sweep: mobile + web + backend)

> Requested by Kira ahead of tomorrow's tester. Sources: flow-graph diff (backend routes vs
> mobile calls), stub/marker grep across `mobile/src` and `web/launch`, screen-by-screen checks,
> `docs/QA-2026-07-18.md`, `docs/LAUNCH_CHECKLIST.md`.
> Triage: 🔴 tester-facing (tonight) · 🟡 this week, before widening beta · 🟢 July backlog / parity.
> Items marked **[verify]** need a device/prod check before coding.
>
> Verified NOT broken during this audit (no action): cancellation penalty preview (25%/50% shown),
> invoice PDF (tokened URL, wired), web StatusPage (real `/health` call), contact form (Resend),
> flow graph itself (0 orphans, 0 broken edges/API).

## 🔴 P0 — tester hits these tomorrow (overnight dispatch tonight)

| # | Item | Evidence / fix direction |
|---|---|---|
| 1 | Client cannot **reject a quote** — accept only | `PATCH /interests/{id}/reject` exists, never called; `QuoteComparisonScreen.js` has no decline action. Business quotes hang pending forever. Add reject button + optimistic list removal. |
| 2 | Client cannot **cancel/delete a posted job** | `DELETE /service-posts/{post_id}` exists, never called. Tester will post a mistake and be stuck 7 days. Add delete on MyJobs post card. |
| 3 | Client cannot **edit a post** after publishing | No update call from mobile. At minimum: budget, description, photos, date. |
| 4 | **Referral screen is fake** — promises "$10 credit automatically", code is a user-id slice, stats hardcoded 0/$0 | Kira's call: build it properly. Rails: `referrals` table (code, referrer_id, referee_id, status, credit_cents), code claim at signup (mobile + web field), counters on `GET /me/referrals`. Money/discount application can stay off — but stats must be real and copy honest until credits go live. Web launch reuses the same endpoints (see #43/#44). |
| 5 | **Business→client review UI missing** | Backend `reviews.py` fully supports `reviewee_type=client`; `ReviewSubmitSheet.js` is built but mounted nowhere. Mount on business JobManagement post-completion. |
| 6 | **Disputes are write-only** | Can file (`POST /disputes/`) but `GET /disputes/mine` never called — no way to see status. Add "My disputes" list (Profile or ActiveBooking). |
| 7 | **Notifications Center misses everything that happens while app is closed** | AsyncStorage-only (`swingby_notifications`); server events never land in it. Feed it from booking events / unread endpoints on open, merge with local. |
| 8 | **Employee logins get full owner powers** | `App.js:77` routes `employee` into `BusinessNavigator` — earnings, employee mgmt, business edit all reachable. Scope navigator by role (employee = assigned jobs + messages + profile). |
| 9 | **AdminScreen is a stub** (106 lines, 0 API calls) | Backend has `/admin/users`, `/admin/bookings`, suspend/unsuspend, force-complete — none surfaced. Needed to moderate the beta. Minimal list + action buttons. |
| 10 | **Escrow state invisible to client** | `payments` tracks escrow_held/released; no UI shows "funds held → 50% released → released". Kira confirmed client-pays-on-confirm; show the milestones on ActiveBooking/BookingDetails. |
| 11 | **Profile photo upload = "coming soon"** but everything exists | `POST /uploads/image` + `users.avatar_url` both live. Wire `ProfileEditScreen.js:63`. |
| 12 | **Dispute photo upload = "coming soon"** — same | Wire `DisputeFlowScreen.js:210` to `/uploads/image`. |
| 13 | QA-03 Android address input (already queued) | Keep top of tonight's dispatch — blocks posting on Android. |
| 14 | QA-06 unified quote→booking thread (already queued) | Design intent in CLAUDE.md; history must survive the handshake. |
| 15 | QA-07/QA-08 handoff-fidelity screens (already queued) | BusinessProfile (owner + public), JobManagement, client MyJobs. |

## 🟡 P1 — this week, before widening beta

| # | Item | Evidence / fix direction |
|---|---|---|
| 16 | Favorites are device-local (AsyncStorage) | Lost on reinstall, no cross-device, web Favorites can't share. Add `favorites` table + GET/POST/DELETE; migrate local on first sync. |
| 17 | No single-post detail screen | `GET /service-posts/{post_id}` never called; MyJobs is list-only. Needed once posts get photos/edits. |
| 18 | `/interests/mine` omits `image_urls` | Known from QA-05 triage — bites as soon as quoted-post detail exists (#17). |
| 19 | Reviews received are invisible | `GET /reviews/client/{client_id}` never called; clients can't see their reputation, businesses can't vet clients. Reviews-list UI both sides. |
| 20 | No token refresh | `POST /auth/refresh` unused; JWT expiry mid-session hard-logs-out (api.js 401 handler fires). Silent refresh interceptor. |
| 21 | Sign-out never invalidates server-side | `POST /auth/logout` unused; token stays valid after logout. |
| 22 | PaymentMethodScreen is a Stripe stub | Either hide entry for beta or wire SetupIntent saved cards. Don't show a dead screen to testers past tomorrow. |
| 23 | No refund path anywhere | DB allows `payment_status=refunded` (migration shipped) and `PATCH /disputes/{id}/resolve` exists — neither surfaced. Dispute resolution needs refund action (admin). |
| 24 | **Business payouts don't exist** | No Stripe Connect / bank details; "released_to_business" has nowhere to go. Decide beta workaround (manual e-transfer, tracked via mark-paid-offplatform) + roadmap Connect onboarding. |
| 25 | Business can't dismiss/archive opportunities in feed | Feed accumulates noise; add dismiss (client-side hide is fine v1). |
| 26 | Business portfolio/gallery photos missing | `BusinessProfileScreen.js:187` photo placeholder; uploads endpoint exists. Public profile sells the business — needed before wider beta. |
| 27 | License verification has no UI on either end | `license_status` is manual: business can't upload a license doc, admin can't verify (ties to #9). Minimal: upload → admin toggle. |
| 28 | Earnings: chart stub + CSV export "coming soon" alert | `EarningsScreen.js:191`. Implement server CSV (web BusinessExports already calls API — reuse) or remove the button. |
| 29 | BusinessAnalytics category chart stubbed | Native-View bars stand-in pending chart lib; acceptable if labeled, but "Analytics coming soon" empty state needs real threshold logic. |
| 30 | Push end-to-end on Android **[verify]** | Backend already sends (`send_push_to_user` on interests/bookings/events); mobile registers tokens. Confirm physical-device delivery + FCM creds (launch checklist #5). |
| 31 | Deep links **[verify]** | Checklist wants `swingby://booking/<id>` + `swingby://invite/<code>`; `linking.js` has no invite handling. Wire invite→signup-with-code once #4 lands. |
| 32 | No post-completion loop: "Book again" / tip | After review the flow dead-ends. Rebook-same-business is the cheapest growth loop we're missing. |
| 33 | Suspended-account UX missing | Backend can suspend; mobile has no dedicated handling — user just sees generic errors. Detect and show a suspended screen. |
| 34 | Email verification consistency **[verify]** | Web has `/verify-email`; confirm mobile signup + unverified-login behavior match (employees are created `email_confirm: true` — clients/businesses?). |
| 35 | Offline UX | api.js has retry interceptor but no user-visible offline banner/queue. |
| 36 | Empty-states audit | Sweep every list screen (Favorites, Disputes-once-built, Search no-results, Analytics) for designed empty states. |
| 37 | Arabic locale is skeleton keys + RTL untested | `i18n.js:246` "translator TODO". Either finish AR or hide it from the language picker for beta — a half-translated RTL app looks broken. |
| 38 | Notification read-state split-brain | `GET /messages/unread-count` drives badges but Notifications Center keeps its own AsyncStorage read flags — unify with #7. |

## 🟢 P2 — July backlog / web parity & polish

| # | Item | Evidence / fix direction |
|---|---|---|
| 39 | Web SavedSearches = 12-line stub | Build against a real endpoint or drop from nav until real. |
| 40 | Web NotificationSettings = 10-line stub | Same treatment. |
| 41 | Web PaymentMethods = 12-line stub | Same treatment; align with #22. |
| 42 | Web BusinessIntegrations decorative (166 lines, 0 API calls) | Label honestly "coming soon" or hide. |
| 43 | Web dashboard has **no referral page** | Mobile has one; once #4 rails exist, add web page on same endpoints. |
| 44 | Web signup lacks referral-code field | Pairs with #4/#31 invite deep link. |
| 45 | HowItWorks pages ship placeholder mockup frames | `AppMockupFrame` labels, no real screenshots — press/tester-facing; replace with real captures. |
| 46 | Maintenance page ETA TODO(HUMAN) | `Maintenance.jsx:22`. |
| 47 | Web ↔ mobile parity audit | Web dashboard lacks: quote comparison, disputes, post edit; mobile lacks: services catalog (web BusinessServices is wired!), exports. Decide the parity line, write it down. |
| 48 | SEO/blog content review | `data/seo-content.js` drives category/location pages — review claims before indexing. |
| 49 | FEATURE-01 route optimization (parked) | Keep parked; needs product design (already in brain PLAN). |
| 50 | Surface license/verified badge in search + profiles | `license_status` exists but clients never see trust signals in browse/search results. |
| 51 | Business hours/availability model absent from schema | Clients can't see open hours or book against availability — schema + UI, post-beta. |
| 52 | Per-business service catalog on mobile | Web `BusinessServices.jsx` already calls API; mobile shows nothing of it. |
| 53 | App version check / force-update mechanism | No kill-switch if a bad build ships to testers. |
| 54 | Accessibility pass | Mobile labels partial (SearchField has them); web has an AccessibilityPage making promises — audit both. |
| 55 | Chat abuse guards | Pre-booking chat is live (QA-06 design) — no rate limit / contact-info-leak filter; undermines the interests spam-shield + escrow. |
| 56 | Product analytics events in mobile **[verify]** | Confirm whether any funnel instrumentation exists; add post→quote→accept→complete funnel events before wider beta. |
| 57 | Launch-checklist debt scheduled | CORS allow-list, Sentry, HIBP toggle, real Maps key, RLS-on-12-tables check, store privacy disclosures — all still ☐; assign to July days rather than launch-eve. |
| 58 | TOOL-01 pre-doctor diagnostic (queued tonight) | Keep visible until it produces its first report. |

## 🔬 P0 — mis-wiring sweep #59–#66 (2026-07-18 evening — Kira: "we have so many errors that dont make sense; i need more")

> Second-pass hunt, different class from the gaps above: features wired on BOTH ends but mis-wired
> in between (param names, response shapes, mismatched keys). Method: code-traced every one of the
> 81 mobile API calls against the backend route's actual return shape/params. All confirmed in code,
> not speculation. Backend suite is green (52 pass) — these live in the seams tests don't cover.
>
> **✅ ALL RESOLVED 2026-07-19 (overnight orchestrator; bus 20260719-2005/2006/2007).** #59 D1 unwrap→`items` (release-blocking, verified: no `data?.businesses` remains) · #60 D2 profile envelope (prior cycle) · #61 D3 `e.user`→`e.users` · #62 D4 analytics reviews by business id+reviewee_type (+test) · #63/#64 D5 preferred_date persisted + derived title≠description (+test; migration `docs/service_posts_preferred_date.sql` FILED, apply is Bucket B) · #65 D6 completed-booking send allowed (+test) · #66 D7 cancel toast uses server penalty · schema doc D8 regenerated (377 lines, reconstructed-from-repo). Gates: pytest 68/3, babel 116/0, flow_graph 0 broken. Working tree only — no push.

| # | Item | Evidence |
|---|---|---|
| 59 | **Home browse + Nearby map render NOTHING once the working tree ships** | `/businesses/nearby` returns `{items:[...]}` (paginated) but `HomeScreen.js:122` and `NearbyMapScreen.js:399` unwrap `data?.businesses` — a key that never exists. Works against old prod only because prod still returns a raw array. The moment Kira pushes, Home + map go permanently empty. Fix unwrap to `data?.items` (SearchScreen already does it right). **RELEASE-BLOCKING.** |
| 60 | **Profile edits appear not to save** | `PATCH /auth/me` returns `{message, user}`; `ProfileEditScreen.js:87` calls `updateUser(updated)` — spreading the ENVELOPE into the auth user (`{...prev, message, user}`), so first/last/phone never update on screen until re-login. Should be `updateUser(updated.user)`. |
| 61 | **Employee search matches nothing** | Backend embeds the join as `users`; `EmployeeManagementScreen.js:493` filter reads `e.user` → name is always `''` → any typed query filters out every employee. |
| 62 | **BusinessAnalytics "recent reviews" always empty** | Business reviews store `reviewee_id = business_id` (reviews.py:58) but analytics queries `reviewee_id = owner user id` (businesses.py:344). Key mismatch → zero rows forever. Backend fix: query by business id + reviewee_type=business. |
| 63 | **PostJob date/time silently discarded** | Wizard collects date + time (state + reset at PostJobScreen.js:637) but the payload sends neither, and `ServicePostCreate` has no such field. Client picks when they want the job; businesses never see it. Needs: column (migration, FILED), model field, payload, render on business post cards. |
| 64 | **Post title = description, always** | `PostJobScreen.js:612` sends `title: description.trim()` AND `description: description.trim()` — every post's title is the full description (MyJobs "super messy" contributor). Wizard has no title field; add one or derive a short title. |
| 65 | **Chat on a completed booking: visible thread, every send fails** | `/messages/threads` lists completed bookings (messages.py:381) but `POST /messages/` rejects status not in (confirmed, in_progress) (messages.py:271) — tester finishes a job, opens the chat, types "thanks", gets a 400. Align: allow sends on completed (or read-only composer). |
| 66 | Cancel-fee toast shows client-computed number; server computes its own penalty (bookings.py:721) and ignores the mobile `penalty_amount` field — numbers can diverge. Use the cancel response's penalty in the toast. | Minor; verify server response includes penalty and render that. |

Also found (docs, not runtime): `docs/swingby_database_schema.md` is a 77-byte stub — CLAUDE.md calls it the schema source of truth. Regenerate from the live schema.

## Dispatch plan

- **Tonight (overnight loop, before tester):** #1–#15, in that order after the already-queued QA items (13–15 first — they block Android testing entirely). #4 scope tonight = real code + signup claim + real counters + honest copy; credit *application* stays off.
- **2026-07-19:** fold 🟡 #16–#38 into the majors backlog alongside tester findings.
- **Week of 07-20:** 🟢 items ride with store-prep; #57 gets distributed onto specific July days.

> **DISPATCHED 2026-07-18 evening:** 🔴 #1–#12 → runner queue as **GAP-1..12** and 🟡 #16–#38 → **GAP Wave 2 (W1–W17)** in `~/brain/projects/swingby/claude/memory/PLAN.md` (Tonight section). #13–#15 were already queued as DQ-5/DQ-4/DQ-6. The overnight orchestrator updates statuses here as items land.
