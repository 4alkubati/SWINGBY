# 2026-07-18 — Saturday · EAS build config + first preview build

## 🎯 WIN CONDITION
- [ ] `eas.json` profiles configured (preview + production)
- [ ] First iOS + Android preview build submitted to EAS

## ☀️ YOU (Kira)
1. `cd mobile && npx eas-cli login` (Kira account).
2. `npx eas-cli init` if not initialized.
3. Kick off `npx eas-cli build --platform all --profile preview`. ~20–30 min per platform.

## 🛌 OVERNIGHT LOOP
- Dispatch: wire icon + splash config drift · confirm bundle IDs match store enrollment.

## ✅ SCORECARD
Two preview builds land in your EAS dashboard.
