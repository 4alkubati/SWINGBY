# 2026-07-02 — Thursday · Smoke test (you) ‖ D2.4 finish (loop)

> Part of [[../July/README|July]] · master timeline [[../DOMINOES]]. Terms → glossary in [[../June|June.md]].

## 🎯 WIN CONDITION
- [ ] Smoke test passes end-to-end on Render with seed creds
- [ ] D2.4 finished + verify loop green (loop)

## ☀️ YOU (Kira) — run the full-chain smoke test
**Pre-reqs:** Render deploy green · 3 seed accounts exist in Supabase (from Jun 27) · Stripe keys in Render.
1. Open Git Bash in the repo: `cd ~/OneDrive/Desktop/AMR/CODE/Swingby`
2. Run (swap in the real passwords from [[../../credentials/test-accounts/seed-accounts|seed-accounts.md]]):
   ```
   BASE_URL=https://swingbyy-api.onrender.com \
   CLIENT_EMAIL=client@swingby.app CLIENT_PASSWORD=SwingbyClient2026 \
   BIZ_EMAIL=business@swingby.app BIZ_PASSWORD=SwingbyBusiness2026 \
   python backend/scripts/smoke_e2e.py
   ```
3. Read the output. It should walk: healthz → signup/login both roles → post → quote → accept → confirm-date → arrived → started → before-photo → completed → after-photo → release → review → events ≥ 3.
4. **Verify:** script exits 0 / prints PASS. Any red step = a bug → file it to tomorrow's [[2026-07-03|Jul 3]] file and tell the loop.

## 🛌 OVERNIGHT LOOP
- Dispatch: [[../dominoes/D2.4-business-subscription|D2.4]] finish + the verify loop (flip a sub to `past_due` in the Stripe test dashboard → confirm the app reflects the gate). Build steps in the D2.4 domino file.

## ✅ SCORECARD
The one that counts: the smoke test goes green against Render — proof the whole booking+payment chain works on the real server.
