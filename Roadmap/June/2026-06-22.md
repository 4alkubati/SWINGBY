# 2026-06-22 — Monday · Beta hardening + push to Render

> Mostly Kira-only items today. Agent runs the deep work overnight via CLI.

---

## 🎯 TODAY = WIN WHEN
- [ ] Backend `/messages/unread-count` is live on Render (no more 503s in mobile)
- [ ] One canonical category list decided + shipped across web + mobile + backend
- [ ] Three personas (admin / client / business) walked end-to-end on localhost:8081 with zero hard errors
- [ ] Apple Developer enrollment started (Google Play optional today)

---

## ☀️ MORNING (before gym) — Kira-only, ~20 min

### 1. Push last night's fixes (~2 min)

These four files are sitting unstaged on `main`:

| File | Purpose |
|---|---|
| `backend/app/api/messages.py` | New `/unread-count` stub endpoint (silences mobile 503 spam) |
| `mobile/src/context/UnreadContext.js` | Polling gated on `user` being logged in |
| `mobile/src/services/api.js` | Interceptor honors `_silent: true` to suppress global toasts |
| `mobile/src/screens/auth/SignupScreen.js` | Password rules (uppercase/lowercase/digit) + visible hint |

```bash
git add backend/app/api/messages.py \
        mobile/src/context/UnreadContext.js \
        mobile/src/services/api.js \
        mobile/src/screens/auth/SignupScreen.js
git commit -m "fix(mobile+backend): unread-count stub, silent polling, signup rules"
git push origin main
```

Watch Render dashboard → wait for "Live" badge (~1-2 min).

### 2. Decide on category names — message me the answer

This is the bug that breaks the entire post → match flow. Two categories don't match between Home and PostJob:

| Home shows | PostJob uses | Decide |
|---|---|---|
| `Electric` ⚡ | `Electrical` | ⬜ Electric ⬜ Electrical |
| `Lawn` 🌱 | `Landscaping` | ⬜ Lawn ⬜ Landscaping |

Other categories already match: Cleaning, Plumbing, Moving, Painting, Carpentry. Plus PostJob has `Handyman` which Home doesn't show.

**Suggested canonical list (8):** Cleaning · Plumbing · Electrical · Landscaping · Painting · Carpentry · Moving · Handyman.

Once you decide, I'll unify everywhere.

### 3. Apple Developer enrollment (~30 min, blocking D3)

[developer.apple.com/programs/enroll](https://developer.apple.com/programs/enroll) → $99/yr → **use real legal name + DUNS lookup**. Review takes 24-48h. **Start it today or D3 slips to Thursday.**

### 4. (Optional) Google Play Console — $25 one-time

[play.google.com/console](https://play.google.com/console). Review usually < 24h.

---

## 🏋️ MID-DAY — gym + comp reading

- 30 min reading 1-star reviews on **HomeStars**, **Handy**, **Uber/Lyft**, **DoorDash**
- One sentence per app into `marketing/comp-frustrations.md`: "what frustrates their users most?"
- Save the top 3 pain points — they go into the post-launch site copy.

---

## 🌙 EVENING LOCK-IN (7:15–9:45)

### Order — execute top to bottom:

1. **Smoke-test the deploy** (5 min)
   - Mobile: shake → Reload Expo Go
   - Sign in as client@swingby.app — confirm zero "Request failed" toasts
   - Confirm welcome email still arrives on a fresh signup (D1 stays green)

2. **Watch the agent finish category unification** (15 min)
   - Review the diff — Home/PostJob/BusinessSetup/JobManagement now import from one constants file
   - Backend has a Pydantic enum + DB constraint
   - Existing seed data migrated to canonical names

3. **Walk all 3 personas on localhost:8081** (30 min)
   - **Admin**: `4alkubati@gmail.com` → lands on AdminScreen → "Open web admin" works → log out
   - **Client**: `client@swingby.app` → Home shows Bob's Cleaning Co. → tap it → Business Profile → back → Post a Job (Cleaning, valid address) → submit → My Jobs
   - **Business**: `business@swingby.app` → Dashboard → Jobs → see Cara's job → quote → confirm

4. **File any new wall** as a finding in `AGENTS/claude/deliverables/chrome-audit-2026-06-22.md`

5. **Close**: update `STATUS.md`, frame Tuesday — likely "EAS scaffolding while Apple reviews".

### Night — overnight CLI runs:
- Brief: `AGENTS/BRIEF-D2-polish.md` (agent writes from today's findings)
- Tasks: photo upload diagnosis, date+time combined picker, Settings polish, avatar upload wiring

---

## ✅ Did I win today?
- [ ] Backend live (no 503s) · [ ] Categories unified · [ ] 3 personas pass · [ ] Apple enrollment in flight
- [ ] STATUS.md updated · [ ] Wednesday framed

---

## Carry-in from last night

What landed in this session:
- D1 ✅ confirmed (welcome email arrives)
- BusinessSetup screen + navigator gate (forces business owners to complete profile)
- AdminScreen + RootNavigator routes role=admin
- LoginScreen "Sign up" link bug fixed (was navigating to Onboarding)
- GooglePlacesAutocomplete moved to named import + web Platform guard
- Mobile `.env` now points at Render (no LAN-IP gotcha)
- Calgary cleaning seed business inserted for audit

What you marked done today (no action needed):
- ✅ Google Places API enabled in Google Cloud Console
- ✅ Supabase Auth: Confirm email + Site URL + redirect URLs
- ✅ DMARC record on swingbyy.com
