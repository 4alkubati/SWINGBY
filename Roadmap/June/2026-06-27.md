# 2026-06-27 — Saturday · clear the 4 gates

## 🎯 WIN CONDITION
- [ ] All 4 human-gate tasks below green
- [ ] `git status` shows "up to date with origin/main"
- [ ] Render env has 4 Stripe vars + a webhook endpoint
- [ ] DMARC TXT record in Cloudflare
- [ ] 3 seed accounts in Supabase Auth (auto-confirmed)

## ☀️ YOU (Kira)

### 1. Push the D2.5 commit *(Bucket C)*
1. `cd ~/OneDrive/Desktop/AMR/CODE/Swingby`
2. `git add Roadmap/ AGENTS/claude/memory/STATUS.md AGENTS/claude/memory/HUMAN-TODO.md AGENTS/claude/PRODUCT-VISION.md CLAUDE.md docs/`
3. `git commit -m "docs(D2.5): rewrite STATUS to reality, lock D2.4, backfill June, add DOMINOES plan"`
4. `git push origin main`

### 2. Stripe keys + 2 products *(Bucket B)*
- `sk_test_…` → Render env `STRIPE_SECRET_KEY`
- Webhook endpoint `https://swingbyy-api.onrender.com/payments/stripe/webhook` with events: `checkout.session.completed`, `customer.subscription.*`, `invoice.payment_succeeded`, `invoice.payment_failed` → `whsec_…` → Render env `STRIPE_WEBHOOK_SECRET`
- Products **Solo $30** + **Team $80** (recurring CAD/mo) → price IDs → Render env `STRIPE_PRICE_SOLO`, `STRIPE_PRICE_TEAM`

### 3. DMARC TXT *(Bucket B)*
Cloudflare → swingbyy.com → DNS → TXT → Name `_dmarc` → Content `v=DMARC1; p=none;`

### 4. Three seed accounts *(Bucket B)*
Supabase → Authentication → Users → Add user (×3) using `credentials/test-accounts/seed-accounts.md`. Tick **Auto Confirm User** on each.

## 🛌 OVERNIGHT LOOP
D2.5 ✅ done → queue [[dominoes/D2.1-employee-trust-card|D2.1]] for tonight.

## ✅ SCORECARD
All 4 gates green so the smoke test + D2.0 can run.
