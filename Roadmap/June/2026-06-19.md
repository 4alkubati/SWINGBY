# 2026-06-19 — Friday · OS day (email + morning debrief, then night OS)

> Operate from this file. Morning = professional email + debrief upgrade. Night (after shift) = the OS work.
> ⚠️ Last pure-infrastructure day. After today the machine drops beta dominoes.

---

## ☀️ MORNING — Professional email @swingbyy.com (free, stays in Gmail)

**Route:** Cloudflare *receives* → your Gmail. Resend *sends* (as your domain). $0. No Zoho.

### A. Receiving — Cloudflare Email Routing
- [ ] Cloudflare dashboard → **swingbyy.com** → **Email** → **Email Routing** → **Get started / Enable**.
- [ ] **Destination address:** add your personal Gmail → check Gmail for the verify link → click it.
- [ ] **Custom address:** create `hello@swingbyy.com` → forward to your Gmail. (Add `support@` too if you want.)
- [ ] Cloudflare auto-adds the **MX + SPF** records → accept them.
- [ ] Test: email `hello@swingbyy.com` from your phone → it should land in Gmail.

### B. Sending — Gmail "Send mail as" (via Resend SMTP)
> Needs your Resend domain = Verified first (from Domino 1). Resend is the sender.
- [ ] Resend → **API Keys** → copy a key (or reuse the app one) → password manager.
- [ ] Gmail → **Settings** → **Accounts and Import** → **Send mail as** → **Add another email address**.
- [ ] Name: `SwingBy` · Email: `hello@swingbyy.com` · (untick "treat as alias" if you want a clean from).
- [ ] SMTP server: `smtp.resend.com` · Port: `587` · Username: `resend` · Password: your **Resend API key** · TLS.
- [ ] Gmail sends a verification code → it arrives in your Gmail (via Cloudflare routing) → enter it.
- [ ] Test: compose in Gmail, set "From" = `hello@swingbyy.com`, send to yourself → confirm it shows as your domain.

### C. DNS coexistence (don't break email)
- [ ] **One** SPF TXT record that includes BOTH Cloudflare routing and Resend (merge the includes — don't make two SPF records).
- [ ] Keep Resend's **DKIM** record. Cloudflare's **MX** = receiving. No conflict when both are present correctly.

### D. Wire it in (so it's used)
- [ ] App: set `hello@swingbyy.com` as contact/support + CASL email footer + Resend reply-to.
- [ ] Socials: use `hello@swingbyy.com` to own/register IG, FB, etc.

**Morning win:** you can send + receive pro email at `@swingbyy.com`, free, from your normal Gmail.

---

## ☀️ MORNING (part 2) — Upgrade the morning debrief
- [ ] In n8n → **Compile Brief** node: add a step that reads today's dated file (`Roadmap/June/<date>.md`) and includes a **TODAY: morning / night** section in the brief.
- [ ] So each morning's brief shows: build status + waitlist + *today's plan*.

---

## 🌙 NIGHT (after shift) — the OS work
- [ ] **n8n:** confirm morning brief fires on schedule; sketch the next automation (gen-2 idea).
- [ ] **Agent runs:** confirm `run-overnight.sh` works; decide morning vs night agent flow.
- [ ] **Templates:** finalize how the daily debrief is generated (this dated-file format + the n8n pull).
- [ ] **Roadmap:** read `Roadmap/README.md` top to bottom; adjust June sprint if needed.
- [ ] **Close:** update STATUS.md → frame tomorrow (first beta domino day).

---

## ✅ Did I win today?
- [ ] Pro email works (send + receive)
- [ ] Debrief shows today's plan
- [ ] OS night tasks done → tomorrow = dominoes
