---
type: domino
id: D2.1
status: done
phase: 1 — BETA
started: 2026-06-28
done: 2026-08-02
links: [[../DOMINOES]]
prev: [[D2.0-live-walkthrough]]
next: [[D2.2-invoices]]
tags: [domino, mobile, backend, schema, trust]
---

# 🁢 D2.1 — Employee trust card to BusinessProfile parity

> Index: [[../DOMINOES|DOMINOES]] · Prev: [[D2.0-live-walkthrough|D2.0]] · Next: [[D2.2-invoices|D2.2 — Invoices]] · Master log: [[_LEARNING-LOG]]

## 🎯 Goal

[[../../mobile/src/screens/business/EmployeeProfileScreen|EmployeeProfileScreen]] shows the same caliber of trust signals as [[../../mobile/src/screens/business/BusinessProfileScreen|BusinessProfileScreen]]: real avg rating + count, "Verified by {Business}" badge, "{N} jobs completed", "Member since {Mon YYYY}".

## 🤔 Why this comes next

Trust is the product (per [[../../AGENTS/claude/PRODUCT-VISION|PRODUCT-VISION]]). Right now a client tapping an employee sees stars showing "—" and no verification — that's the opposite of trust. This is a small, contained, high-leverage win that's safe to do first because it touches no payment logic.

## ✅ Pre-reqs

- [ ] [[D2.0-live-walkthrough|D2.0]] complete OR walked through far enough to confirm no other employee-profile crashes hiding underneath.
- [ ] Supabase MCP access (project `ulnxapnsenzyddddldjt`).
- [ ] Read `[[../../docs/swingby_database_schema|swingby_database_schema.md]]` to confirm current `employees` + `reviews` + `bookings` shapes.

## 🪜 Step-by-step

### 1. Backend — add aggregation endpoint
File: `[[../../backend/app/api/employees.py]]` (new or extend existing).

Endpoint:
```
GET /employees/{employee_id}/profile
→ {
  id, user_id, business_id, role_title, is_active, joined_at,
  avg_rating: float | null,
  review_count: int,
  jobs_completed: int,
  verified_via_business: bool,        # mirrors businesses.license_status == 'verified'
  business_name: str
}
```

Implementation notes:
- avg_rating + count: `SELECT avg(rating), count(*) FROM reviews WHERE reviewee_id = :uid AND reviewee_type = 'employee'`.
- jobs_completed: `SELECT count(*) FROM bookings WHERE employee_id = :eid AND status = 'completed'`.
- verified_via_business: join `businesses.license_status`.
- joined_at: `employees.created_at`.
- Auth: any authenticated user can read.

**Verify:** `curl -H "Authorization: Bearer ..." https://swingbyy-api.onrender.com/employees/<id>/profile` returns the shape.

### 2. Backend — register route in [[../../backend/app/main.py|main.py]]
Add `app.include_router(employees.router, prefix="/employees", tags=["employees"])` if not already there.
**Verify:** `/openapi.json` shows the route.

### 3. Mobile — extend [[../../mobile/src/screens/business/EmployeeProfileScreen|EmployeeProfileScreen]]
- Swap the placeholder fetch for `GET /employees/{id}/profile`.
- Render in the existing identity card:
  - Stars (use existing `RatingStarsDisplay`) + count next to it ("4.8 · 23 reviews").
  - If `verified_via_business`: render a `Badge` "Verified by {business_name}" (success color).
  - Stats row → 3 boxes: rating · jobs_completed · "Since {Mon YYYY}".
- Loading state: skeleton already exists, just align the new fields.
- Empty state: if `review_count === 0`, show "No reviews yet — first one earns a badge."

### 4. Mobile — link from [[../../mobile/src/screens/business/EmployeeManagementScreen|EmployeeManagementScreen]]
Already navigates to EmployeeProfile — sanity check the navigation params include `employeeId`.

### 5. Manual test
- Log in as `testclient`, navigate to a business with employees, tap an employee.
- **Verify:** trust card renders all 4 fields. No "—" placeholders. Verified badge shown only when business is verified.

### 6. Commit
```
git add backend/app/api/employees.py backend/app/main.py mobile/src/screens/business/EmployeeProfileScreen.js
git commit -m "feat(D2.1): employee trust card — ratings, verified, jobs_completed, tenure"
git push origin main
```
Render auto-deploys; wait for `/openapi.json` to show the new route.

## 🏁 Done-rule

- [x] `GET /employees/{id}/profile` live on Render — prod returns `401` (gated); an unrouted path on the same API returns `404`. *Response shape not verified — needs an authenticated call.*
- [ ] EmployeeProfileScreen renders all 4 trust fields with real data. *Screen exists and is wired; visual confirmation is a device check → D6.4.*
- [x] No "—" placeholders left on the screen — verified: zero `"—"` literals in `EmployeeProfileScreen.js`.

## 📖 Log (append-only)

### YYYY-MM-DD — first entry template
- What you did:
- What broke:
- What you decided:

### 2026-06-28 — backend endpoint + mobile rewrite (code-complete, awaits push + on-device verify)
- What was done:
  - **Backend** — added `GET /employees/{employee_id}/profile` to `backend/app/api/employees.py`. Auth: any logged-in user. Returns `{id, user_id, business_id, role_title, is_active, joined_at, first_name, last_name, avatar_url, avg_rating, review_count, jobs_completed, verified_via_business, business_name}`. Aggregates: reviews where `reviewee_id = employees.user_id AND reviewee_type='employee'`, bookings where `employee_id = :id AND status='completed'`, businesses.license_status for the verified flag.
  - **Mobile** — rewrote `mobile/src/screens/business/EmployeeProfileScreen.js`. Single call to `/employees/{id}/profile` replaces the prior `/employees/` list-then-filter + business-reviews mis-fetch. Identity card now shows: Avatar, name + role, real rating stars + `4.8 · N reviews` line when reviews exist, "✓ Verified by {business_name}" success pill when verified, 3-box stats row (Rating · Jobs · "Since {YYYY/Mon}"). No "—" placeholder fallbacks except the rating-when-zero which now shows `0.0` honestly. Empty-state copy aligned to spec: "No reviews yet — first one earns a badge."
- What was flagged (parked, not loop-runnable):
  - **`reviews.reviewee_type` CHECK constraint allows only `('client','business')`** — `'employee'` rejected. So the new aggregation returns 0 reviews today. The endpoint shape is correct for when the client→employee review flow ships (extend CHECK + add submission path); separate domino.
- What was decided:
  - Removed the prior reviews-list section (was wrongly showing business reviews under the employee). Replaced with the empty-state from spec + a "list coming with the client→employee review flow" placeholder when `review_count > 0` (which today is unreachable; reserved slot).
  - Kept the existing skeleton + retry button as-is — both already match the new field shape after stat-row rewrite.
- Verification:
  - Python AST clean on `employees.py` ✅
  - FastAPI boot with stub env → 65 routes, `/employees/{employee_id}/profile [get]` visible in `/openapi.json` ✅
  - Babel parse clean on `EmployeeProfileScreen.js` ✅
  - Cross-file: `BusinessProfileScreen.js:502` already navigates with `{ employeeId: emp.id, businessId: resolvedId }` — params match the new screen ✅
- Outstanding for done-rule (Bucket C/B):
  - Commit + push (Kira) → Render auto-deploys → confirm `/openapi.json` shows the new route on prod.
  - On-device verify in Expo Go: log in as client, open a business with employees, tap an employee → all 4 trust fields render, verified pill conditional, no "—".


### 2026-08-02 — reality sync: this was finished and never ticked

Audited against `origin/main` + live prod, not against the previous note.

- `GET /employees/{id}/profile` — **live and auth-gated**: prod returns `401`,
  where an unrouted path on the same API returns `404`. Defined at
  `backend/app/api/employees.py:341`.
- `mobile/src/screens/business/EmployeeProfileScreen.js` exists and is wired.
- Placeholder sweep: **zero** `"—"` literals left in that screen.

**Status corrected `in-progress` → `done`.** It had sat `in-progress` since
2026-06-28 while the work shipped underneath it.

**Not verified here:** that the four trust fields *visually* render with real
data on a device. That is a screen check, and it belongs to the D6.4 pre-spend
sweep — not to a doc audit. Its box stays unticked on purpose.

## 🎓 Learning

- _to fill as you go_
