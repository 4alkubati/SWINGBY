# Development and testing dependencies for SwingBy backend
# Install with: pip install -r requirements-dev.txt

# Testing frameworks
pytest>=9.1.1
pytest-asyncio>=0.21.0

# HTTP testing
httpx>=0.24.0
respx>=0.20.0

# SQL validation (for migration tests)
sqlparse>=0.4.4

# Load testing via k6 (install separately: https://k6.io/docs/getting-started/installation/)
# k6 is a binary, not a Python package
