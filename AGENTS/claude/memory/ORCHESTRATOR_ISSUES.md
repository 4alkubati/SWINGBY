---
name: orchestrator-issues
description: Comprehensive SwingBy bug, gap, and improvement tracker — generated by cross-platform audit (backend + mobile + web + design)
metadata:
  type: project
---

# SwingBy — Orchestrator Issue Tracker
> Generated: 2026-06-15 | Last audited: full cross-platform pass (backend, mobile, web/launch, web/admin, design)
> Status icons: ✅ Done  🔄 In progress  ❌ Not started  ⚠️ Needs attention

---

## CRITICAL — Blocking core user flows

| ID | Layer | Issue | Status | Fix |
|---|---|---|---|---|
| C1 | Mobile | PostJobScreen: no photo upload | ✅ Fixed 2026-06-15 | expo-image-picker + `/uploads/image` endpoint |
| C2 | Mobile + Backend | PostJobScreen: address collected but never sent to backend | ✅ Fixed 2026-06-15 | Added `address` field to ServicePostCreate + mobile payload |
| C3 | Mobile | QuoteComparisonScreen: needs real data (list interests from `/interests/post/{id}`) | ✅ Already done | Fully wired — calls GET /interests/post/{postId}, sort by rating/price, ConfirmSheet |
| C4 | Backend | `/api-keys` CRUD endpoints missing (web/launch ApiKeys.jsx is wired but 404s) | ❌ | Create `backend/app/api/api_keys.py` + migration |
| C5 | Backend | `GET /businesses/me/analytics` missing (BusinessAnalyticsScreen + web rely on it) | ❌ | Create analytics aggregation endpoint |
| C6 | Backend | `POST /webhooks` missing (BusinessIntegrations.jsx tries to register webhooks) | ❌ | Create webhooks table + endpoints |
| C7 | Mobile | HomeScreen: browse/nearby not wired to real `/businesses/nearby` API | ❌ | Replace mock data with API call |
| C8 | Mobile | Business DashboardScreen: no real data (shows placeholders) | ❌ | Wire GET /bookings + GET /businesses/me |
| C9 | Mobile | ChatScreen / MessageThreadScreen: polling not implemented | ❌ | Wire GET /messages/{booking_id} with interval polling (5s) |

---

## HIGH — Should fix before public launch

| ID | Layer | Issue | Status | Fix |
|---|---|---|---|---|
| H1 | Mobile | Google Maps API key in plain text in app.json | ⚠️ Exists | Move `android.googleMapsApiKey` to EAS secret via `eas secret:create` |
| H2 | Backend/Auth | Admin password "Swingby123" is temporary | ⚠️ | Rotate via Supabase Auth dashboard |
| H3 | Auth | HaveIBeenPwned leaked-password protection not enabled | ⚠️ | Enable in Supabase Auth → Password → Enable leaked password protection |
| H4 | Email | Resend not configured — all transactional emails are silent | ❌ | Sign up for Resend → verify swingbyy.com → set RESEND_API_KEY in backend env |
| H5 | Mobile | EarningsScreen: placeholder data only | ❌ | Wire GET /payments (sum by business + month) |
| H6 | Mobile | BusinessAnalyticsScreen: all mock charts | ❌ | Wire GET /businesses/me/analytics (blocked on C5) |
| H7 | Mobile | EmployeeManagementScreen: list loads but create/deactivate not wired | ❌ | Wire POST /employees + PATCH /employees/{id}/deactivate |
| H8 | Mobile | PaymentMethodScreen: shows UI but Stripe not integrated | ⚠️ | Post-MVP: Stripe Connect |
| H9 | Mobile | ReferralScreen: shows referral code but no backend sharing endpoint | ❌ | Post-MVP or generate code client-side from user.id |
| H10 | Mobile | NotificationsScreen / NotificationsCenterScreen: no real push data | ❌ | Wire push_tokens + unread context to show real notifications |
| H11 | Backend | `supabase-storage`: bucket `job-photos` needed for image uploads | ✅ Fixed 2026-06-15 | Created via SQL + RLS policies |
| H12 | Backend | `service_posts` missing `image_urls` and `address` columns | ✅ Fixed 2026-06-15 | Applied migration |

---

## MEDIUM — Polish and correctness

| ID | Layer | Issue | Status | Notes |
|---|---|---|---|---|
| M1 | Mobile | react-native-reanimated version in package.json shows `~4.1.1` but MOBILE_FIX_PROMPT says use v3 | ⚠️ | Audit if any v4-only APIs are used; Reanimated 4 changed the plugin setup |
| M2 | Mobile | expo-notifications warnings on startup (acceptable but clutters logs) | ⚠️ | Add `experienceId` to notifications config in app.json |
| M3 | Mobile | ProfileEditScreen: exists but navigation from ProfileScreen uses inline edit not a separate screen | ❌ | Either remove ProfileEditScreen or route to it from Profile header |
| M4 | Mobile | SearchScreen: no API call — input not wired to `/businesses/nearby?search=` | ❌ | Wire search to businesses endpoint with debounce |
| M5 | Mobile | FavoritesScreen: list is local state only, not persisted | ❌ | Add `favorites` table + endpoints OR use AsyncStorage |
| M6 | Mobile | SavedSearches: no backend (implied from useFavorites hook) | ❌ | Persist to AsyncStorage or backend |
| M7 | Mobile | MapScreen + NearbyMapScreen: require Google Maps key (shows blank without it) | ⚠️ | Add fallback / instructions |
| M8 | Mobile | PostJobScreen: Step 0 allows advancing without selecting a category (defaults to 'General') | ⚠️ | Require category selection on Step 0 OR show warning |
| M9 | Mobile | SignupScreen: if email confirmation is ON in Supabase, user lands on blank screen after signup | ❌ | Add "Check your email" confirmation screen |
| M10 | Mobile | LoginScreen: no "Forgot password?" link | ❌ | Add navigation to ForgotPassword screen (screen exists) |
| M11 | Backend | Several endpoints still use broad `except Exception` → str(e) leaks possible | ⚠️ | Audit + replace with structured logger (most fixed in wave 4, verify) |
| M12 | Backend | `GET /businesses/nearby` returns 200 with empty list when no key — geocode params not validated | ⚠️ | Add param validation for lat/lng |
| M13 | Web/launch | `BusinessAnalytics.jsx`: fetches `/businesses/me/analytics` — returns 404 until C5 fixed | ⚠️ | Add loading + graceful empty state |
| M14 | Web/launch | `ApiKeys.jsx`: all API calls 404 until C4 fixed | ⚠️ | Add "Coming soon" state |
| M15 | Web/admin | `AuditLogPage.jsx`: hits `/admin/audit-log` which may 404 | ⚠️ | Verify endpoint exists; add empty state |
| M16 | Backend | `notion_crm.py` sync on signup is untested in production | ⚠️ | Add try/except so Notion failure doesn't block signup |
| M17 | Supabase | Email confirmation ON but mobile app doesn't handle the confirmation callback | ❌ | Add deep-link handler `swingby://auth/confirm` |

---

## LOW — Nice to have before v1.1

| ID | Layer | Issue | Status | Notes |
|---|---|---|---|---|
| L1 | Mobile | i18n scaffolded (EN/FR/AR) but zero strings translated | ❌ | Post-launch priority |
| L2 | Mobile | Detox E2E: only signup.e2e.js — no coverage for post-job, booking, messaging | ❌ | Add before App Store submission |
| L3 | Mobile | `useShake` hook imported but not wired to any screen | ⚠️ | Wire to error states or remove |
| L4 | Mobile | No haptic feedback on form submission or error | ❌ | Use `expo-haptics` on success/error in PostJob + Login |
| L5 | Backend | Rate limiter thresholds not tuned (e.g., 5/min on auth may be too restrictive for testing) | ⚠️ | Tune after load testing |
| L6 | Backend | No automated Supabase database backup | ❌ | Enable in Supabase dashboard (Project → Database → Backups) |
| L7 | Web | Plausible analytics script uses `swingbyy.com` — update to `swingbyy.com` | ⚠️ | Update src in Layout.jsx |
| L8 | Web | Cloudflare Web Analytics not enabled | ❌ | Add snippet to `_headers` / layout |
| L9 | CI/CD | EAS Build not in CI — mobile builds triggered manually only | ❌ | Add `eas build` step to GitHub Actions workflow |
| L10 | CI/CD | `web-launch-ci.yml` doesn't run `npm test` (no test files in web/launch) | ⚠️ | Add Playwright smoke test step |
| L11 | Security | Admin role assignment is manual SQL — no automated invite flow | ⚠️ | Add `POST /admin/invite` endpoint |
| L12 | Docs | `RUNNING_LOCALLY.md` mentions `GOOGLE_MAPS_API_KEY_PLACEHOLDER` — needs real key instructions | ⚠️ | Update docs |

---

## DESIGN — Visual parity with wireframe (home-screen-ali.html)

| ID | Screen | Issue | Status | Notes |
|---|---|---|---|---|
| D1 | Global | Wireframe uses orange accent (#FF5C00) — impl uses purple (#6E56F7) | ⚠️ | Decision needed: keep purple OR switch to orange |
| D2 | HomeScreen | Wireframe has "Good morning, Ali." greeting — impl may not | ❌ | Add personalized greeting with first name |
| D3 | HomeScreen | Mode switcher (Browse / Post a Job) — needs Uber-style tab look | ⚠️ | Polish ModeSwitch component styling |
| D4 | HomeScreen | Notification bell with orange dot not implemented | ❌ | Wire unread count to bell icon in header |
| D5 | HomeScreen | "Top Rated" featured card with glow/badge — missing | ❌ | Add FeaturedCard with top-rated business |
| D6 | HomeScreen | Location pill "📍 Calgary, AB" — not shown in header | ❌ | Add location pill to HomeScreen header |
| D7 | PostJobScreen | Photo upload box now added with dashed border | ✅ Fixed 2026-06-15 | |
| D8 | PostJobScreen | Wireframe is single-screen form; impl is 4-step wizard | ⚠️ | Wizard is better mobile UX — keep but match visual style |
| D9 | NearbyCard | Distance pill (📍 3.4 km) not showing real distance | ❌ | Compute from user location + business lat/lng |
| D10 | CategoryScroll | Active state needs orange-border style from wireframe | ⚠️ | Already has accent styling — verify Chip active state matches |

---

## ALREADY FIXED (reference)

| ID | Issue | Fixed |
|---|---|---|
| F1 | Logout button broken | Already wired in ProfileScreen — was likely old bug |
| F2 | MyJobs blank page | Has proper loading/error/empty states |
| F3 | PostJob → QuoteComparison navigation | navigation.navigate('QuoteComparison', ...) is wired |
| F4 | RLS on all 12 tables | Applied via MCP (0 warnings) |
| F5 | str(e) leaks in backend | 26 fixed in Wave 4 |
| F6 | service_posts.image_urls column | ✅ Added 2026-06-15 |
| F7 | service_posts.address column | ✅ Added 2026-06-15 |
| F8 | /uploads/image endpoint | ✅ Created 2026-06-15 |
| F9 | Mobile PostJobScreen photo upload | ✅ Implemented 2026-06-15 |
| F10 | LoginScreen Forgot password? was no-op | ✅ Fixed 2026-06-15 — navigates to ForgotPasswordScreen |
| F11 | ForgotPasswordScreen missing | ✅ Created 2026-06-15 + POST /auth/forgot-password endpoint |
| F12 | QuoteComparisonScreen already fully implemented | ✅ Confirmed 2026-06-15 — was marked as C3 in error |

---

## MESSAGE BUS — Active dispatches

```
---
ID: 2026-06-15-001
FROM: orchestrator
TO: frontend-agent
TYPE: REQUEST
PRIORITY: HIGH
SUBJECT: Wire QuoteComparisonScreen to GET /interests/post/{post_id}
BODY:
  QuoteComparisonScreen (mobile/src/screens/QuoteComparisonScreen.js) receives
  { postId, postTitle } from navigation params. It must call:
    GET /interests/post/{postId}
  which returns an array of interest objects (business quotes).
  Each row should show: business name, quoted_price, status pill.
  Client taps "Accept" → PATCH /interests/{id}/accept → creates booking.
  Must have: loading skeleton, empty state ("No quotes yet — check back soon"),
  error state with retry, pull-to-refresh, and haptic on accept.
STATUS: OPEN
---

---
ID: 2026-06-15-002
FROM: orchestrator
TO: backend-agent
TYPE: REQUEST
PRIORITY: HIGH
SUBJECT: Create GET /businesses/me/analytics endpoint
BODY:
  Required by BusinessAnalyticsScreen (mobile) and BusinessAnalytics.jsx (web).
  Should return (for the authenticated business owner):
    - total_earnings (sum of released_to_business from payments)
    - total_bookings (count)
    - avg_rating (from businesses.avg_rating)
    - bookings_by_month (last 6 months, for chart)
    - top_categories (top 3 from service_posts linked to this business)
  Route: GET /businesses/me/analytics
  Auth: business_owner or employee role only
STATUS: OPEN
---

---
ID: 2026-06-15-003
FROM: orchestrator
TO: frontend-agent
TYPE: REQUEST
PRIORITY: NORMAL
SUBJECT: Add HomeScreen real data (greeting + nearby businesses API call)
BODY:
  HomeScreen should:
  1. Show "Good morning/afternoon/evening, {first_name}." based on hour
  2. Fetch GET /businesses/nearby?lat={lat}&lng={lng}&radius_km=15
     using expo-location for user coordinates
  3. Show skeleton while loading, then FeaturedCard (top-rated) + NearbyCard list
  4. Empty state if no businesses found ("No businesses nearby yet")
  5. Show location pill in header (city name from reverse geocode or static "Calgary, AB")
STATUS: OPEN
---

---
ID: 2026-06-15-004
FROM: orchestrator
TO: database-agent
TYPE: REQUEST
PRIORITY: NORMAL
SUBJECT: Create api_keys table + backend endpoints
BODY:
  web/launch/src/pages/app/ApiKeys.jsx expects:
    GET /api-keys      → list user's API keys (id, name, prefix, created_at, last_used_at)
    POST /api-keys     → create key (name) → returns full key ONCE
    DELETE /api-keys/{id} → revoke key

  Migration needed:
    CREATE TABLE api_keys (
      id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      user_id uuid REFERENCES users(id) ON DELETE CASCADE,
      name text NOT NULL,
      key_hash text NOT NULL,  -- bcrypt hash of the key
      key_prefix text NOT NULL, -- first 8 chars for display (e.g. "swb_a1b2")
      last_used_at timestamptz,
      created_at timestamptz DEFAULT now()
    );
    Enable RLS: users can only see/delete their own keys.

  Backend: backend/app/api/api_keys.py
STATUS: OPEN
---
```

---

## NEXT ACTIONS (in priority order)

1. **User action**: Enable HaveIBeenPwned in Supabase Auth → Password
2. **User action**: Move Google Maps key to EAS secrets (`eas secret:create`)
3. **User action**: Sign up for Resend → verify swingbyy.com → set `RESEND_API_KEY` env var
4. **Dispatch C3**: Wire QuoteComparisonScreen (MESSAGE BUS 2026-06-15-001)
5. **Dispatch C5**: Create `/businesses/me/analytics` endpoint (MESSAGE BUS 2026-06-15-002)
6. **Dispatch C7**: HomeScreen real data + greeting (MESSAGE BUS 2026-06-15-003)
7. **Dispatch C4**: api_keys table + endpoints (MESSAGE BUS 2026-06-15-004)
8. **Dispatch M9**: SignupScreen email confirmation handler
9. **Dispatch M10**: LoginScreen "Forgot password?" link
10. **Design decision**: Orange (#FF5C00) vs Purple (#6E56F7) accent — get user approval
