VITE_API_URL=https://api.swingbyy.com

# PROD_API_URL placeholder for full backend (post Render deploy):
# VITE_BACKEND_API_URL=https://swingby-api.onrender.com  # PLACEHOLDER — swap on deploy

# Public launch is October. While this is true (or unset), swingbyy.com serves the
# waitlist only. Set to false at launch to bring the full site back — nothing is deleted.
VITE_PRELAUNCH_GATE=true
